---
id: TASK_0002
state: failed
phase: critique
created_at: 2026-07-20T08:41:40.655Z
updated_at: 2026-07-20T09:05:32.654Z
title: Pick up the Phase 4 deploy window from the handoff at `/tmp/handoff-phase4-2026-07-20.md` and drive it to closure: implement the 5 installer fixes surfaced by the halo150 clean-install (Hermes PATH, NFS group detection, `/dev/tty` guard, `hindsight-api` health-check race, locale warnings) without colliding with fixes owned by other parallel sessions, run the four remaining migration windows on halo143 (FLAGS-own migrator, Honcho→Hindsight, P2-updater-b, P3-runtime-db), execute the deploy rehearsal (SLOT-B live flip + golden-paths runbook), live-validate both boxes (halo150 + halo143), then close the Phase 5 release cut (version bump to v1.0.0 across pyproject + ui/package.json + ui/index.html in one commit, CHANGELOG rewrite, README + dashboard screenshot refresh, Honcho→Hindsight + slot-id-flip upgrade notes, `v1.0.0` tag on main). Both boxes currently run `main` at `e3163605` / version `0.9.8`; the in-place upgrade on halo143 must preserve its existing data and the 1,303 model-layout symlinks.
label: Phase 4 deploy closure + Phase 5 v1.0.0 release cut (halo150/halo143)
reason: model_error in critique: Codex error: The usage limit has been reached
---

## raw prompt

Document written to /tmp/handoff-phase4-2026-07-20.md.                        
                                                                               
 Covers:                                                                       
                                                                               
 - Box state — both boxes running main at e3163605, halo143 handoff referenced 
   separately                                                                  
 - 5 installer fixes from the clean‑install on halo150 — with severity and     
   suggested remediation for each (Hermes PATH, NFS group detection, /dev/tty  
   guard, hindsight‑api health check race, locale warnings)                    
 - Gitops discipline — the capped gate, push‑wave protocol, CI red =           
   stop‑the‑line, migration windows as live orchestration                      
 - Phase 4 remaining — four migration windows, deploy rehearsal, live‑validate 
 - Phase 5 release prep — version bump, CHANGELOG, screenshots, upgrade notes, 
   tag                                                                         
 - Key artifacts with read‑order                                               
 - Suggested skills for the next session          - pickup handoff and implement fixes w/o running into the other sessions fixes

## refined prompt

GOAL
Pick up the Phase 4 deploy window from the handoff at `/tmp/handoff-phase4-2026-07-20.md` and drive it to closure: implement the 5 installer fixes surfaced by the halo150 clean-install (Hermes PATH, NFS group detection, `/dev/tty` guard, `hindsight-api` health-check race, locale warnings) without colliding with fixes owned by other parallel sessions, run the four remaining migration windows on halo143 (FLAGS-own migrator, Honcho→Hindsight, P2-updater-b, P3-runtime-db), execute the deploy rehearsal (SLOT-B live flip + golden-paths runbook), live-validate both boxes (halo150 + halo143), then close the Phase 5 release cut (version bump to v1.0.0 across pyproject + ui/package.json + ui/index.html in one commit, CHANGELOG rewrite, README + dashboard screenshot refresh, Honcho→Hindsight + slot-id-flip upgrade notes, `v1.0.0` tag on main). Both boxes currently run `main` at `e3163605` / version `0.9.8`; the in-place upgrade on halo143 must preserve its existing data and the 1,303 model-layout symlinks.

CONSTRAINTS
- Do NOT touch CT105 (10.0.1.142) — it is the untouched live reference (hal0 0.9.8, render gid 993) and must remain comparable.
- Box access only via the listed SSH invocations: PVE host `ssh -i ~/.ssh/thin-mint root@10.0.1.110`; halo150 via `ssh -i ~/.ssh/thin-mint root@10.0.1.110 'pct exec 150 -- bash'`; halo143 via `ssh -o StrictHostKeyChecking=no root@10.0.1.143`.
- Both halo150 (CT 150, 10.0.1.150) and halo143 (CT 143, 10.0.1.143) must stay on `main` `e3163605` / `0.9.8` until the Phase 5 release-cut commit. Do NOT bump version ahead of Phase 5.
- Gitops discipline (unchanged from drive-2 + Phase-4 handoff): capped gate on every merge (`ruff check` + `ruff format --check` + import smoke + `scripts/check_sunset.py` + targeted pytest <90s for Python; `tsc` + `eslint` + `build` + targeted γ for UI); push waves batch 2–4 lane merges with consolidated full-suite gate before each push; CI red on `descar`/`main` = stop-the-line — fix forward, NEVER rewrite history.
- One integration branch `rework/descar`; merges to `main` only at phase boundaries.
- Migration-window lanes (FLAGS-own, Honcho→Hindsight, P2-updater-b, P3-runtime-db, SLOT-B live flip) are orchestrator-run live steps against real boxes — NEVER dispatch them as worktree agent lanes.
- Deploy-affecting items must be exercised on BOTH boxes (halo150 privileged / halo143 unprivileged) and recorded per box.
- The 5 installer fixes from the halo150 clean-install must NOT collide with fixes owned by other parallel sessions — verify ownership via `graphify` and `using-shepherd` before editing any installer file.
- Fix 1 (Hermes PATH, severity Medium): the provision step's `hal0 agent install hermes` call must use the absolute path `/usr/lib/hal0/venv/bin/hal0` (already resolved earlier in the installer) instead of relying on `$PATH`.
- Fix 2 (NFS group detection, severity High for NFS users): the model-store preflight must detect the owning gid of `/mnt/ai-models` (observed 1000:2000), `groupadd -g 2000 hal0-models` if missing, then `usermod -aG hal0-models hal0`.
- Fix 3 (`/dev/tty` guard, severity Low): `installer/install.sh:2395` must be guarded with `test -t 0` or accept a `--non-interactive` / `--accept-defaults` flag for `pct exec` / CI / scripted contexts.
- Fix 4 (`hindsight-api` race, severity Low): add `sleep 2` + retry, or probe `systemctl is-active` before the HTTP health check.
- Fix 5 (locale warnings, severity Cosmetic): add `apt-get install -y locales && locale-gen en_US.UTF-8` to the bootstrap prereqs.
- Phase 5 release-cut commit MUST contain the version bump in pyproject + ui/package.json + ui/index.html together in a single commit.
- Required skills to invoke: `graphify` (query the knowledge graph before grepping source), `diagnose` / `systematic-debugging` (for any live-box anomaly during the deploy window), `verification-before-completion` (evidence before claims; never assert green without fresh output), `finishing-a-development-branch` (for landing the Phase-5 release-cut PR to main), `using-shepherd` (check past runs before repeating live steps on the boxes).
- The hermes-agent upstream pin at commit `9de9c25f620ff7f1ce0fd5457d596052d5159596` (date 2026-07-07, repo https://github.com/NousResearch/hermes-agent) must remain untouched; drift-watch process is documented in ADR-0018 §4 and the weekly `scripts/hermes-sdk-diff.sh` job.
- SLOT-B live flip must be atomic: `hal0-slot@<name>` → `hal0-slot@<id>` unit rename + podman rename + M5 on real state + runtime path/unit flip must land as one indivisible step.
- Coordinate with the SLOT-B M5 migrator so neither the P3-runtime-db nor the SLOT-B lane double-touches `state.json`.
- Residual build lanes (`typed-bodies-rest`, MCP route-map autogen, FLAGS-own §7 tail) may be parallel worktree agents ONLY if blocked only on these; the MCP route-map autogen is blocked on the §4.4 3-gap addendum (deny-by-default, transport exclusions, re-key special-casing; spec `spec-p3-routers.final.md:514-601`).

KNOWN-UNKNOWNS
- Which of the 5 installer fixes are claimed by other parallel sessions, so the agent can avoid file-level collisions (no ownership map is provided in the handoff).
- Whether `config/migrations/slot_flags_fold.py` has been dry-run against real halo143 state yet — the handoff says "never yet run against real state" but does not confirm whether another session started that.
- Whether the Honcho → Hindsight memory migration has been rehearsed with deterministic Honcho fixtures yet.
- Whether the SLOT-B live flip has been rehearsed end-to-end or only spec'd.
- Exact state of the `rework/descar` branch — the handoff says "all lanes already collapsed to main", but does not state whether the branch itself is live, archived, or safe to delete.
- Which migration-window lanes are owned by the agent vs. the human orchestrator (the split is implied but not enumerated).
- Whether the 1,303 model-layout symlinks on halo143 are stable or being mutated concurrently by another lane.
- Whether the Phase 5 `v1.0.0` tag is gated on live-validate passing on BOTH boxes, or may be staged in parallel.
- Where the suggested skills (`graphify`, `diagnose`, `systematic-debugging`, `verification-before-completion`, `finishing-a-development-branch`, `using-shepherd`) are defined and how they are invocable from this session.
- Whether the MCP route-map autogen is in scope for this session given its hard blocker on the §4.4 3-gap addendum.
- Whether the `typed-bodies-rest` residual lane (15/24 `request.json()` sites need Pydantic typed bodies) should be opened as a board row now or is owned by another session.
- Exact retry/backoff policy for the hindsight-api fix: `sleep 2` + single retry vs. active-then-probe vs. exponential backoff.
- Whether locale-gen must run before or after the bootstrap prereqs check, and whether it should be conditional on the container image distro.
- Whether `docs/rework/handoff-r5-phase4.md` and `.handoff-halo143-phase4.md` (the next two artifacts in the read-order) introduce additional fixes or lanes that supersede items in the handoff.
- Whether the prior validation record `docs/rework/deploy-validation/2026-07-19-r5-install-validation.md` (r5v1) reports any installer fixes already landed that this session should not re-do.

EXTERNAL-DEPENDENCIES
- Proxmox VE  third-party LXC/KVM virtualization platform at root@10.0.1.110 hosting CT 105 / CT 143 / CT 150 used via pct exec
- NousResearch/hermes-agent  third-party agent SDK vendored from https://github.com/NousResearch/hermes-agent at pinned commit 9de9c25f620ff7f1ce0fd5457d596052d5159596

## verified tooling

ruff check src tests
mypy src
pytest tests/ -v
bash scripts/harness.sh
bash scripts/release-test.sh
pip install -e ".[dev]"
cd ui && npm run build
cd ui && npm run lint
cd ui && npm run typecheck
cd ui && npm run test:unit
cd ui && npm run test:e2e
cd ui && npm run test:e2e:install
cd ui && npm run dev
uvicorn hal0.api:app --reload --host 127.0.0.1 --port 8080
docker build -f packaging/toolbox/cpu.Dockerfile .
docker build -f packaging/toolbox/flm.Dockerfile .
docker build -f packaging/toolbox/qwen3tts.Dockerfile .
bash scripts/deploy.sh

## research

FILES
(degraded: research FILES worker stuck in a loop — called find({"limit":100,"path":"/home/mint/hal0/tests","pattern":"**/*honcho*migrat*"}) ×5 in the last 20 calls and still looped after restarts; this section may be incomplete)

APIS
(degraded: research APIS worker stuck in a loop — called find({"limit":100,"path":"/home/mint","pattern":"**/migrate-honcho/SKILL.md"}) ×5 in the last 18 calls and still looped after restarts; this section may be incomplete)

CONTEXT
(degraded: research CONTEXT worker timed out after restarts; this section may be incomplete)

- The current `installer/install.sh:2173` already calls `"${HAL0_BIN}" agent install hermes` where `${HAL0_BIN}=${VENV_DIR}/bin/hal0`, so Fix 1 (Hermes PATH) is partially landed — verify whether `${VENV_DIR}` resolves to `/usr/lib/hal0/venv` or a different path on a `main @ e3163605` install before editing; the explicit task requirement is the absolute `/usr/lib/hal0/venv/bin/hal0`.
- Fix 3 (`/dev/tty` guard, `installer/install.sh:2395` `_confirm_launch_setup`) must gate with `test -t 0` and accept a new top-level `--non-interactive` / `--accept-defaults` flag; grep confirms no such flag currently exists in `installer/`, so both the flag plumbing in `arg parsing` (~L96-110) and the `>`/dev/tty` redirect at L2395+2407 need to be added together — half-fixes won't reach CI/pct-exec contexts.
- Fix 5 (locale warnings) has zero footprint in `installer/` today — no `apt-get install locales` or `locale-gen en_US.UTF-8` exists anywhere under `installer/`. The natural hook is `installer/bootstrap.sh` (top-level) which currently only calls `apt-get update`; the locales step must run BEFORE the Python/hermes-prereqs probes so `LC_ALL` is sane when subsequent child shells inherit it, and should be conditional on the distro family (only Debian/Ubuntu need `locale-gen`).
- Fix 4 (hindsight-api race) targets `installer/install.sh:2037-2043` — the post-`systemctl enable --now hindsight-api` poll loop already exists (3 retries × sleep 3). The race is `systemctl is-active` returning "active" before the HTTP port is bound; the recommended remedy is `wait_active hindsight-api 30` (the project's `wait_active` helper at L62-77) followed by a single `curl /health` probe, not bare `sleep 2 + retry` which can mask a real bind failure.
- Fix 2 (NFS group detection, severity High for NFS users) targets `installer/lib/preflight.sh`; the existing preflight has GPU-group detection (L761-775) but NO logic for `${MODELS_DIR}`'s owning group. The fix must detect `stat -c '%g'` on `${MODELS_DIR}`'s mount-point, `groupadd -g <gid> hal0-models` if missing, then `usermod -aG hal0-models hal0`, AND must run only when `${MODELS_DIR}` is non-FHS (i.e. matches `/mnt/*` or similar), to avoid disrupting the FHS-default `/var/lib/hal0/models` path.
- Of the 5 installer fixes, M2 (keyring-EDQUOT diagnostic in `_container_runtime_gate`) and the m3 GPU-gid fix in `providers/_gpu.py` are ALREADY landed on `main` (per `installer/lib/preflight.sh:484-498` and `REWORK_BOARD.md` row `m3-renderer` lane `9fa55044`); do not duplicate them. Verify ownership via `graphify query "keyring install preflight"` and `git log --oneline -- installer/lib/preflight.sh` before touching these files — they are likely claimed by other lanes.
- The handoff's "5 fixes" overlap with `rework/descar` lane commits `205d5262`/`0b4e6786`/`6d154bf1`/`b9c0b0e5`/`6f8266d5` (M1-M5 in `.handoff-halo143-phase4.md`); those are on `rework/descar` tip `ea3e4661` / `9951a5aa`, NOT on `main @ e3163605`. The orchestrator must decide which branch to land the release-cut version bump on first — bumping on `rework/descar` and PR-promoting differs from bumping on `main` directly.
- `rework/descar` is "all lanes collapsed to main" per handoff §1, but tip `ea3e4661` is 5+ commits ahead of `main @ e3163605` per the shepherd transcript evidence (PR #1326 board sync, PR #1325 §7 chat_template fold, workload-profile seeds `54a5dc68`, LocalAI analysis). PR #1318 is the descar→main promotion PR; check its mergeable state before assuming the branch can be force-reset.
- Phase 5 release-cut version bump is a SINGLE commit across three files: `pyproject.toml:13` (`version = "0.9.8"`→`"1.0.0"`), `ui/package.json:4` (`"version": "0.9.8"`→`"1.0.0"`), and `ui/index.html` is build-stamped (DO NOT hand-edit per the in-file comment L17-20 — bump only `ui/package.json`, the `hal0-version-html` vite plugin handles `index.html` on the next build). Cherry-picking the wrong set breaks dashboard flash.
- The `slot_flags_fold` migrator dry-run ALREADY executed on both boxes (stamp r5v3) and REFUSED on both: 143 `qwen3.5-0.8b` (3 slots: brain ctx=32000, qtest ctx=4096, smoke-test ctx=8192), 150 `hal0-brain-fpx8-agent` (2 slots: agent `-fa on` ctx=64000, brain full-ROCm ctx=64000). Manual canonicalization via SqliteModelRegistry ALREADY DONE: 143 `qwen3.5-0.8b-brain` row + `brain.toml [model].default`=`qwen3.5-0.8b-brain`; 150 `hal0-brain-fpx8-agent.defaults`=`-fa on -dev Vulkan0`, plus `hal0-brain-fpx8-agent-brain` split. Residual refusal: 143 `qtest vs smoke-test` on `context_size` — but `migrator-ctx` lane `e99a0c2d` already dropped `context_size` from `FoldedTune` per REWORK_BOARD L216, so this residual SHOULD clear on next dry-run (untested on live boxes post-merge).
- A FULL migrator apply (`deploy_window=True`) on 143 would now abort cleanly (refusal: qtest vs smoke residual) and on 150 would PROCEED but clobber the manual canonicalization by ADDING `context_size=64000` to `hal0-brain-fpx8-agent.defaults` (the fold-write mechanism is `existing ⊕ fold`, it doesn't skip non-empty defaults). Per r5v3 recommendation: do NOT run a full apply; the targeted manual restore is sufficient and the §2 regression scope is fully closed on both boxes (only 143-brain, 150-agent, 150-brain were regressed; all three restored).
- The Honcho→Hindsight migration is a NO-OP on both boxes: `hal0 memory status` shows provider=`durable`, engine=`hindsight`, only `hindsight-api.service` active (no honcho unit). The P2-memory migration window per REWORK_BOARD L230 is `☐ todo` but its payload does not exist on these substrates. The "seed deterministic Honcho fixtures" rehearsal step is moot — flag this as a removed-from-scope item rather than blocking on it.
- The SLOT-B M5 id-flip (`migrate_slot_id_keying` in `src/hal0/slots/migrate_id_keying.py`) is BLOCKED on runtime-version sequencing per the live r5v3 attempt on 143 (stamp idflip1): the deployed 0.9.8 runtime is NAME-keyed, re-seeds name-keyed slots on boot, and produces split-brain (18 runners: 11 id-keyed ERROR + 7 re-seeded name-keyed) when the layout flips. Restored from backup. The idkey-inc4 lane `f7e498ad` shipped the `hal0 slot migrate-id-keying` CLI with `--dry-run`/`--yes` and pre-flight backup, AND the idkey-core bilingual-read layer — so the v1.0.0 runtime that consumes `<id>.toml`/`<id>/state.json` must deploy FIRST, THEN the flip can be re-attempted. Do NOT re-run the live flip on 0.9.8; do NOT re-run on 150 either (same runtime).
- The SLOT-B M5 migrator has NO pure dry-run mode (only `RecordingSlotArtifactOps` stubs systemd/podman; TOML/state moves are REAL); atomicity is per-file via tmp+`os.replace`, no rollback path — only idempotent re-run-forward recovery. Pre-flight backup MUST cover `/etc/hal0/slots`, `/var/lib/hal0/slots` state dirs, `/etc/containers/systemd` quadlets, AND `hal0.db` (the r5v3 pre-canonicalize backup at `/var/lib/hal0/backups/pre-canonicalize-r5v3/` covers hal0.db + TOMLs but NOT state dirs nor quadlets — take a fresh one before any live attempt).
- The P3-runtime-db migration window (`☐ todo`) touches `state.json` which the M5 id-flip ALSO touches — per REWORK_BOARD L279-281 this is a known double-touch risk; the decision (fold slot-state into SLOT-B vs scope M5) was deferred to "decide at SLOT-B dispatch." Since the live flip is now blocked on runtime sequencing, the decision default should be: M5 owns the rename, P3-runtime-db scopes AROUND it (skip slot-state on first pass; later migration 006 picks it up).
- The migration windows (FLAGS-own, Honcho→Hindsight, P2-updater-b, P3-runtime-db) are EXPLICITLY orchestrator-run live steps per REWORK_BOARD L226 ("NOT agents"). They must never be dispatched as worktree agent lanes — the orchestrator owns the live execution and the rollback discipline.
- The `idkey-inc4` CLI gate requires `hal0-api` stopped + no active `hal0-slot@*` units + the pre-flight backup tar present; the sequencing on halo143 (when runtime ships) is: stop API → backup → migrate-id-keying --yes → daemon-reload → restart API → verify slots resolve by-id. The current `0.9.8` runtime cannot satisfy this — skip until v1.0.0 runtime is in place.
- The `slot_id` values are pre-assigned by Increment-A in `hal0.db` `slot` table (agent=1, brain=2, embed=3, flm=4, img=5, rerank=6, tts=7, utility=8, vision=9, qtest=11, smoke-test=12; id 10 gap = prior-deleted slot). The M5 migrator reuses these — do NOT mint new ids. The bilingual-read layer in `slots/layout.py` accepts both name and id stems, so the live 0.9.8 runtime keeps working from name-stems while the id-keyed runtime reads id-stems; no second write is needed pre-flip.
- SSH access recipe: PVE host `ssh -i ~/.ssh/thin-mint root@10.0.1.110`; halo150 via `pct exec 150 -- bash` (NOT direct SSH); halo143 via `ssh -o StrictHostKeyChecking=no root@10.0.1.143` (direct). Witnessed: a direct `ssh root@10.0.1.150` and direct `ssh root@10.0.1.143` both fail with "Permission denied (publickey,password)" — must use the listed invocations verbatim.
- CT105 (`10.0.1.142`) is the UNTOUCHED live reference — hal0 0.9.8, render gid 993→`render` (matches dev0), `/api/health` 200. NEVER deploy to, NEVER mutate. Used only as the known-good render-gid baseline against which halo143's gid-993=`clock` anomaly is judged.
- The hermes-agent upstream pin at commit `9de9c25f620ff7f1ce0fd5457d596052d5159596` (date 2026-07-07) lives in `installer/agents/requirements.txt`; drift-watch process is in `scripts/hermes-sdk-diff.sh` + ADR-0018 §4. Do not touch this pin in the Phase-4 fixes — it is OUT OF SCOPE for the 5 installer fixes.
- The skill `graphify` exists at `.claude/skills/graphify/SKILL.md` and `.codex/skills/graphify/SKILL.md` (mirrored). The skill `using-shepherd` is referenced in the handoff but not in `.claude/skills/` — the actual local session-history index is `.pi/shepherd/index.json` (multiple `pi-mrs*` run records visible) and `.pi/shepherd/runs/*.json` contain prior transcripts (e.g. `pi-mrsz49h2-7ogvmn.json` is the prior Phase-4 handoff write, `pi-mrsyrzvs-emne90.json` is the box-version probe). Skills `diagnose`, `systematic-debugging`, `verification-before-completion`, `finishing-a-development-branch` are NOT in `.claude/skills/` — they must be invoked by their external skill names (or equivalent) per the agent's own runtime, not as project-local skills.
- The `typed-bodies-rest` ambiguity per `handoff-r5-phase4.md` §3 has TWO open items: (a) `P3-routers (inc 3)` 15 sites, CLOSED via PR #1322 per REWORK_BOARD L133; (b) `typed-bodies-rest` 24 sites in 12 other route files, owned by NO lane — has not been opened as a board row. If the orchestrator meant (a), it is already closed. If (b), open a new board row first. The handoff explicitly warns "don't conflate the two counts."
- The MCP route-map autogen (`build_admin_route_map(app)`) is per REWORK_BOARD L133 CLOSED via PR #1323 on 2026-07-20 — NOT in scope. The §4.4 3-gap addendum (deny-by-default, transport exclusions, re-key special-casing) was settled inline.
- The `FLAGS-own §7 tail` (slot `device`/`mtp` field removal) per REWORK_BOARD L201 is SUPERSEDED by `spec-hw-slot-ownership.md` — only `chat_template` fold shipped (PR #1325); `slot.device` removal was CANCELLED (was task #23). The `mtp` axis is also held; the sunset ratchet retains these as `v1.0.0` stamps without removing the fields.
- The sunset ratchet is now retargeted to `v1.0.0` per commit `0a0bc6e8` — every `# HAL0-SUNSET: v1.0.0` stamp (e.g. `n_gpu_layers`, `extra_args`, `parallel`, `chat_template`, `workers`, `runtime`, `--backend`/`--provider` aliases, `slot add`/`slot remove`, `model register`/`model import`/`model assign`, `import-backup`, `upstream set-credentials`, `hardware --refresh`) will be dropped in this release. CHANGELOG must enumerate them; the ratchet gate is `scripts/check_sunset.py` in the capped CI gate.
- CI env ≠ `uv.lock` is a known flake class (drive-2 §J) — never assert on `app.routes` shape in tests; probe routes behaviorally. The `golden-paths-early` tests at `tests/golden_paths/` are the behavioral acceptance bar.
- `rework/descar` carries concurrent-writer risk (a Claude-authored commit `471c365a` already landed mid-session on 2026-07-19 that the prior handoff session had also drafted). Before dispatching any Phase-4 lane: `git fetch origin && git log --oneline HEAD..origin/rework/descar` and reconcile before any merge that touches files the other session might also touch (FLAGS-own/`argv.py`/tests).
- The 1,303 model-layout symlinks on halo143 are stable (no concurrent mutator on this lane); backup at `/var/lib/hal0/backups/pre-canonicalize-r5v3/` covers `hal0.db` + slots + legacy registry. The 143 phase-3 backup at `/var/lib/hal0/backups/pre-idflip-143-idflip1/` covers state dirs + quadlets + TOMLs + hal0.db (hal0.db sha `8347e2…`) — restore=cp -a + `systemctl restart hal0-api`.
- The `1870-line` Hermes provisioning workflow (`installer/agents/hermes-prereqs.sh`) is now idempotent + distro-aware via `installer/lib/distro.sh`; it already handles `python3.14` → `uv` fallback via `_provision_python_via_uv` if `uv` is on PATH. Fix 1 (absolute path) does not interact with this prereq script — the path change is in `install.sh:2173` only.
- The `hindsight-api` install gate (`install.sh:1988-2066`) pins `hindsight-api==0.7.2` and supports a `--ignore-requires-python` fallback for Python 3.14+ boxes; the `poll loop` at L2037-2043 is the race target for Fix 4. The

VERIFIED-TOOLING
  ruff check src tests
  mypy src
  pytest tests/ -v
  bash scripts/harness.sh
  bash scripts/release-test.sh
  pip install -e ".[dev]"
  cd ui && npm run build
  cd ui && npm run lint
  cd ui && npm run typecheck
  cd ui && npm run test:unit
  cd ui && npm run test:e2e
  cd ui && npm run test:e2e:install
  cd ui && npm run dev
  uvicorn hal0.api:app --reload --host 127.0.0.1 --port 8080
  docker build -f packaging/toolbox/cpu.Dockerfile .
  docker build -f packaging/toolbox/flm.Dockerfile .
  docker build -f packaging/toolbox/qwen3tts.Dockerfile .
  bash scripts/deploy.sh

## grill Q&A

Q1: How should the SLOT-B live flip be sequenced given research shows it is blocked on the v1.0.0 runtime (the deployed 0.9.8 runtime re-seeds name-keyed slots and split-brains on flip), but the task orders it BEFORE the Phase 5 release-cut version bump — (a) defer the live flip to post-v1.0.0 and run only the golden-paths runbook on 0.9.8 as the deploy rehearsal, (b) reverse the order so release-cut source bump → v1.0.0 deploy to both boxes → SLOT-B live flip → live-validate → v1.0.0 tag, or (c) skip SLOT-B entirely this session and ship only the v1.0.0 source bump + tag (treating the live flip as a v1.0.0.x follow-up)?
A1: (a) defer the SLOT-B live flip to a post-v1.0.0 follow-up and run only the golden-paths runbook on 0.9.8 as the deploy rehearsal — keeps the rehearsal + live-validate coverage on 0.9.8, lets the release-cut commit land cleanly, and honors the hard research block ("Do NOT re-run the live flip on 0.9.8 ... the v1.0.0 runtime ... must deploy FIRST, THEN the flip can be re-attempted"); document the deferral in CHANGELOG + ADR-0018 §4 as a v1.0.0.x follow-up so the idkey-inc4 CLI + bilingual-read layer are exercised on the next live flip attempt.
Q2: Should the Phase 5 release-cut commit (the single commit bumping pyproject + ui/package.json + ui/index.html to v1.0.0) and the 5 installer fixes land directly on main, or on rework/descar first and then promoted to main via PR #1318 — given rework/descar is 5+ commits ahead of main (M1-M5 lane commits plus a concurrent Claude-authored commit 471c365a) and PR #1318's mergeable state is unverified?
A2: land on rework/descar first (after reconciling the Claude-authored 471c365a concurrent-writer commit via git fetch origin && git log --oneline HEAD..origin/rework/descar), then promote via PR #1318 at the Phase-5 phase boundary — the constraints explicitly mandate "One integration branch rework/descar; merges to main only at phase boundaries" and the Phase-5 release cut IS a phase boundary; PR #1318 is already the designated descar→main promotion PR

## spec

GOAL
Drive the Phase 4 deploy window to closure from the handoff at `/tmp/handoff-phase4-2026-07-20.md`: land the 5 installer fixes surfaced by the halo150 clean-install (Hermes PATH via absolute `/usr/lib/hal0/venv/bin/hal0`, NFS gid detection for `/mnt/ai-models`, `/dev/tty` guard with a `--non-interactive` flag, `hindsight-api` race fix via `wait_active` + curl probe, locale warnings via `locale-gen en_US.UTF-8`); execute the four remaining migration windows on halo143 (FLAGS-own `slot_flags_fold` — already dry-run + manually canonicalized, do NOT full-apply per r5v3; Honcho→Hindsight — NO-OP on both boxes per `hal0 memory status`; P2-updater-b; P3-runtime-db scoping around the deferred SLOT-B id-flip); run the deploy rehearsal as the golden-paths runbook on 0.9.8 (SLOT-B live flip deferred to a v1.0.0.x follow-up per the human's Q1 answer — the deployed 0.9.8 runtime re-seeds name-keyed slots and split-brains on flip); live-validate both boxes (halo150 + halo143) and record per-box results; then close the Phase 5 release cut on `rework/descar` → PR #1318 → `main`: single version-bump commit covering `pyproject.toml` (v0.9.8 → v1.0.0) + `ui/package.json` (v0.9.8 → v1.0.0) + freshly rebuilt `ui/index.html` via the `hal0-version-html` vite plugin, CHANGELOG rewrite enumerating every `HAL0-SUNSET: v1.0.0` stamp plus the SLOT-B deferral note, README + dashboard screenshot refresh, Honcho→Hindsight + slot-id-flip upgrade notes, and the `v1.0.0` tag on `main`. Both boxes must remain on `main @ e3163605` / `0.9.8` until the release-cut lands; CT105 (10.0.1.142) is the untouched live reference and must remain comparable.

CONSTRAINTS
- Do NOT touch CT105 (10.0.1.142) — it is the untouched live reference (hal0 0.9.8, render gid 993) and must remain comparable.
- Box access only via the listed SSH invocations: PVE host `ssh -i ~/.ssh/thin-mint root@10.0.1.110`; halo150 via `ssh -i ~/.ssh/thin-mint root@10.0.1.110 'pct exec 150 -- bash'`; halo143 via `ssh -o StrictHostKeyChecking=no root@10.0.1.143`. Direct `ssh root@10.0.1.150` and direct `ssh root@10.0.1.143` both fail with "Permission denied (publickey,password)" — use the listed invocations verbatim.
- Both halo150 (CT 150, 10.0.1.150) and halo143 (CT 143, 10.0.1.143) must stay on `main @ e3163605` / `0.9.8` until the Phase 5 release-cut commit lands. Do NOT bump version ahead of Phase 5.
- Land the Phase-5 release cut on `rework/descar` first (after reconciling the Claude-authored `471c365a` concurrent-writer commit via `git fetch origin && git log --oneline HEAD..origin/rework/descar`), then promote via PR #1318 at the Phase-5 phase boundary. "One integration branch `rework/descar`; merges to `main` only at phase boundaries" stands — the Phase-5 release cut IS a phase boundary.
- `rework/descar` carries concurrent-writer risk — Claude-authored commit `471c365a` already landed mid-session on 2026-07-19. Before any Phase-4 lane: `git fetch origin && git log --oneline HEAD..origin/rework/descar` and reconcile before any merge that touches files the other session might also touch (FLAGS-own / `argv.py` / tests).
- Gitops discipline (unchanged from drive-2 + Phase-4 handoff): capped gate on every merge (`ruff check` + `ruff format --check` + import smoke + `scripts/check_sunset.py` + targeted pytest <90s for Python; `tsc` + `eslint` + `build` + targeted γ for UI); push waves batch 2–4 lane merges with consolidated full-suite gate before each push; CI red on `descar`/`main` = stop-the-line — fix forward, NEVER rewrite history.
- Migration-window lanes (FLAGS-own, Honcho→Hindsight, P2-updater-b, P3-runtime-db, SLOT-B live flip) are orchestrator-run live steps against real boxes per REWORK_BOARD L226 — NEVER dispatch them as worktree agent lanes. The SLOT-B live flip is OUT OF SCOPE for this session and must be deferred to a v1.0.0.x follow-up; the deployed 0.9.8 runtime re-seeds name-keyed slots and split-brains on flip (live r5v3 attempt idflip1 produced 18 runners: 11 id-keyed ERROR + 7 re-seeded name-keyed), so the v1.0.0 runtime must deploy FIRST, THEN the flip can be re-attempted via the `hal0 slot migrate-id-keying --yes` CLI from the `idkey-inc4` lane (`f7e498ad`).
- Deploy-affecting items must be exercised on BOTH boxes (halo150 privileged / halo143 unprivileged) and recorded per box.
- The 5 installer fixes must NOT collide with fixes owned by other parallel sessions — verify ownership via `graphify query "keyring install preflight"` (skill at `.claude/skills/graphify/SKILL.md` and mirrored at `.codex/skills/graphify/SKILL.md`) and `git log --oneline -- installer/lib/preflight.sh installer/install.sh installer/bootstrap.sh` before editing. M2 (keyring-EDQUOT diagnostic in `_container_runtime_gate`, landed per `installer/lib/preflight.sh:484-498`) and the m3 GPU-gid fix in `providers/_gpu.py` (landed via `9fa55044`) are ALREADY on `main` — do NOT duplicate.
- Fix 1 (Hermes PATH, severity Medium): the `hal0 agent install hermes` call in `installer/install.sh` around L2173 must use the absolute path `/usr/lib/hal0/venv/bin/hal0` (resolved earlier in the installer) instead of relying on `$PATH`. Verify `${VENV_DIR}` resolves to `/usr/lib/hal0/venv` on a `main @ e3163605` install before editing — Fix 1 is partially landed if `${HAL0_BIN}=${VENV_DIR}/bin/hal0` already resolves correctly; the explicit task requirement is the absolute `/usr/lib/hal0/venv/bin/hal0`.
- Fix 2 (NFS group detection, severity High for NFS users): the model-store preflight in `installer/lib/preflight.sh` must detect the owning gid of `${MODELS_DIR}` via `stat -c '%g'` on the mount-point (observed 1000:2000), `groupadd -g 2000 hal0-models` if missing, then `usermod -aG hal0-models hal0`. The fix must run ONLY when `${MODELS_DIR}` matches `/mnt/*` (or non-FHS), to avoid disrupting the FHS-default `/var/lib/hal0/models` path. Do NOT duplicate the existing GPU-group detection at L761-775.
- Fix 3 (`/dev/tty` guard, severity Low): `installer/install.sh:2395` `_confirm_launch_setup` must be guarded with `test -t 0`, and a NEW top-level `--non-interactive` / `--accept-defaults` flag must be plumbed through the `arg parsing` block (~L96-110) AND consumed at L2395 + L2407 to skip the `> /dev/tty` redirect in scripted/pct-exec/CI contexts. Half-fixes won't reach CI.
- Fix 4 (`hindsight-api` race, severity Low): replace the bare poll at `installer/install.sh:2037-2043` with the project's `wait_active hindsight-api 30` helper (defined at L62-77) followed by a single `curl /health` probe. Do NOT use bare `sleep 2 + retry` — that masks a real bind failure.
- Fix 5 (locale warnings, severity Cosmetic): add `apt-get install -y locales && locale-gen en_US.UTF-8` to `installer/bootstrap.sh` (top-level), BEFORE the Python/hermes-prereqs probes so `LC_ALL` is sane for subsequent child shells. Make it conditional on Debian/Ubuntu distro family via `installer/lib/distro.sh` (RHEL-family skips `locale-gen`).
- Honcho→Hindsight migration is a NO-OP on both boxes: `hal0 memory status` shows `provider=durable, engine=hindsight`, only `hindsight-api.service` active (no honcho unit). Document the NO-OP in CHANGELOG + ADR-0018 §4 rather than blocking on the seed-deterministic-Honcho-fixtures rehearsal step.
- `slot_flags_fold` full apply (`deploy_window=True`) is REJECTED per r5v3: 143 would abort cleanly (residual `qtest vs smoke-test` on `context_size`, expected to clear on next dry-run post the `migrator-ctx` lane `e99a0c2d` merge that dropped `context_size` from `FoldedTune`) and 150 would clobber the manual canonicalization by ADDING `context_size=64000` to `hal0-brain-fpx8-agent.defaults`. Per r5v3 recommendation, do NOT run a full apply; the targeted manual restore is sufficient and the §2 regression scope is fully closed on both boxes (only 143-brain, 150-agent, 150-brain were regressed; all three restored).
- P3-runtime-db must scope AROUND the deferred SLOT-B M5 id-flip per REWORK_BOARD L279-281 (skip slot-state on first pass; later migration 006 picks it up).
- The 1,303 model-layout symlinks on halo143 are stable (no concurrent mutator on this lane) — preserve them through any in-place operation; backup at `/var/lib/hal0/backups/pre-canonicalize-r5v3/` covers `hal0.db` + slots + legacy registry. The 143 phase-3 backup at `/var/lib/hal0/backups/pre-idflip-143-idflip1/` covers state dirs + quadlets + TOMLs + hal0.db (hal0.db sha `8347e2…`) — restore=`cp -a` + `systemctl restart hal0-api`.
- Phase 5 release-cut commit MUST contain the version bump in `pyproject.toml` + `ui/package.json` + freshly built `ui/index.html` together in a single commit. `ui/index.html` is build-stamped by the `hal0-version-html` vite plugin (per in-file comment L17-20) — do NOT hand-edit; run `cd ui && npm run build` after bumping `ui/package.json` and commit the refreshed `ui/index.html` in the same release-cut commit.
- Sunset ratchet is retargeted to `v1.0.0` per commit `0a0bc6e8` — every `HAL0-SUNSET: v1.0.0` stamp (`n_gpu_layers`, `extra_args`, `parallel`, `chat_template`, `workers`, `runtime`, `--backend`/`--provider` aliases, `slot add`/`slot remove`, `model register`/`model import`/`model assign`, `import-backup`, `upstream set-credentials`, `hardware --refresh`) will be dropped in this release. CHANGELOG must enumerate them; the ratchet gate is `scripts/check_sunset.py` in the capped CI gate.
- The hermes-agent upstream pin at commit `9de9c25f620ff7f1ce0fd5457d596052d5159596` (date 2026-07-07, repo https://github.com/NousResearch/hermes-agent) lives in `installer/agents/requirements.txt` and MUST remain untouched; drift-watch process is documented in ADR-0018 §4 + the weekly `scripts/hermes-sdk-diff.sh` job. Drift-watch is OUT OF SCOPE for the 5 installer fixes.
- Required skills to invoke: `graphify` (query the knowledge graph before grepping source — local skill at `.claude/skills/graphify/SKILL.md` + mirror at `.codex/skills/graphify/SKILL.md`), `diagnose` / `systematic-debugging` (for any live-box anomaly during the deploy window — invoke by external skill name from the agent's own runtime), `verification-before-completion` (evidence before claims; never assert green without fresh output), `finishing-a-development-branch` (for landing the Phase-5 release-cut PR to main), `using-shepherd` (check `.pi/shepherd/index.json` and `.pi/shepherd/runs/*.json` — `pi-mrsz49h2-7ogvmn.json` is the prior Phase-4 handoff write, `pi-mrsyrzvs-emne90.json` is the box-version probe — before repeating live steps on the boxes).
- CI env ≠ `uv.lock` is a known flake class (drive-2 §J) — never assert on `app.routes` shape in tests; probe routes behaviorally. The `golden-paths-early` tests at `tests/golden_paths/` are the behavioral acceptance bar.

ACCEPTANCE
- All 5 installer fixes are landed on `rework/descar` (Hermes PATH via absolute `/usr/lib/hal0/venv/bin/hal0`, NFS gid detection, `/dev/tty` guard + `--non-interactive` flag, `wait_active hindsight-api` + curl probe, locale-gen) and pass `bash -n installer/install.sh installer/lib/preflight.sh installer/bootstrap.sh`.
- The 5 installer fixes do NOT duplicate M2 (keyring-EDQUOT in `_container_runtime_gate`) or the m3 GPU-gid fix in `providers/_gpu.py` — verified via `git log --oneline -- installer/lib/preflight.sh installer/install.sh installer/bootstrap.sh providers/_gpu.py` and `graphify query "keyring install preflight"`.
- FLAGS-own `slot_flags_fold` migrator dry-run executed on both boxes; targeted manual canonicalization recorded: 143 `qwen3.5-0.8b-brain` row + `brain.toml [model].default`=`qwen3.5-0.8b-brain`; 150 `hal0-brain-fpx8-agent.defaults`=`-fa on -dev Vulkan0` plus `hal0-brain-fpx8-agent-brain` split. Full `deploy_window=True` apply NOT executed; residual `qtest vs smoke-test` on `context_size` documented as expected-to-clear post `migrator-ctx` `e99a0c2d` merge.
- Honcho→Hindsight migration window recorded as NO-OP on both boxes (`hal0 memory status` shows `provider=durable, engine=hindsight`, only `hindsight-api.service` active); NO-OP recorded in CHANGELOG + ADR-0018 §4.
- P2-updater-b and P3-runtime-db migration windows executed on halo143; P3-runtime-db scoped around the deferred SLOT-B id-flip (slot-state deferred to migration 006).
- Deploy rehearsal ran the golden-paths runbook on 0.9.8 (NOT the SLOT-B live flip); results recorded per box in `docs/rework/deploy-validation/`.
- Live-validate completed on BOTH boxes (halo150 + halo143) pre-release-cut; per-box records include `/api/health` 200, slot layout, model registry, `hal0 memory status`, `hal0 --version` = `0.9.8`, and CT105 baseline preserved.
- `rework/descar` reconciled with the Claude-authored `471c365a` commit before any merge that touches shared files.
- Phase 5 release-cut SINGLE commit lands on `rework/descar` with: `pyproject.toml:13` `version = "1.0.0"`, `ui/package.json:4` `"version": "1.0.0"`, and `ui/index.html` freshly rebuilt via `cd ui && npm run build` (build-stamped by `hal0-version-html` vite plugin) and committed in the same commit.
- CHANGELOG rewritten to enumerate every `HAL0-SUNSET: v1.0.0` stamp and document the SLOT-B live-flip deferral as a v1.0.0.x follow-up; Honcho→Hindsight and slot-id-flip upgrade notes present; README refreshed; dashboard screenshot refreshed.
- PR #1318 promotes `rework/descar` → `main` at the Phase-5 phase boundary; `v1.0.0` tag created on `main`; CI green on `main`.
- Capped gate passes on `rework/descar` tip and on `main` post-merge: `ruff check src tests`, `ruff format --check`, `mypy src`, `python scripts/check_sunset.py`, `pytest tests/golden_paths/ -v`, `bash scripts/harness.sh`, `bash scripts/release-test.sh`; UI `npm run typecheck` + `npm run lint` + `npm run build` + `npm run test:unit` all green.
- Both boxes live-validated post-PR-merge; `/api/health` 200 on each; per-box records updated to version `1.0.0`.

VERIFY:
```sh
bash -n installer/install.sh && bash -n installer/lib/preflight.sh && bash -n installer/bootstrap.sh
ruff check src tests
ruff format --check src tests
mypy src
python scripts/check_sunset.py
pytest tests/golden_paths/ -v
pytest tests/ -v
bash scripts/harness.sh
bash scripts/release-test.sh
pip install -e ".[dev]"
cd ui && npm run typecheck && npm run lint && npm run build && npm run test:unit
grep -nE '^version = "1\.0\.0"' pyproject.toml
grep -nE '"version": "1\.0\.0"' ui/package.json
cd ui && grep -nE '1\.0\.0|hal0-version' index.html | head -n 20
git fetch origin && git log --oneline HEAD..origin/rework/descar
git log --oneline -- installer/lib/preflight.sh installer/install.sh installer/bootstrap.sh providers/_gpu.py | head -n 20
ssh -o StrictHostKeyChecking=no root@10.0.1.143 'hal0 --version; systemctl is-active hal0-api hindsight-api; curl -fsS http://127.0.0.1:8080/api/health; hal0 memory status; ls -la /var/lib/hal0/slots/ | head -n 20; find /var/lib/hal0/backups -maxdepth 2 -type d'
ssh -i ~/.ssh/thin-mint root@10.0.1.110 'pct exec 150 -- bash -lc "hal0 --version; systemctl is-active hal0-api hindsight-api; curl -fsS http://127.0.0.1:8080/api/health; hal0 memory status; ls -la /var/lib/hal0/slots/ | head -n 20; find /var/lib/hal0/backups -maxdepth 2 -type d"'
```

## phase timings

refine               96.7s
research            312.1s
  enrichment              1.8s
  worker:tooling wait    686ms
  worker:tooling work    30.9s
  worker:apis wait       877ms
  worker:apis work       29.4s
  worker:files wait      692ms
  worker:files work     142.9s
  worker:context wait    699ms
  worker:context work   239.3s
  workers               298.2s
  verify-tooling         13.8s
grill               770.5s
  gen                    76.9s
  auto-answer            60.1s
  gen                   198.8s
  auto-answer            24.0s
  gen                    36.4s
  gen                   188.2s
compose             171.9s
critique             71.3s
  triage                 68.0s
  rewrite                 3.3s
total              1422.6s
