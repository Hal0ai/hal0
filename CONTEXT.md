# hal0 — Glossary (moved)

The project glossary now lives in the **Glossary** section of the single
authoritative internal doc: [`ARCHITECTURE.md`](./ARCHITECTURE.md#glossary).

Historical in-code references:

- `CONTEXT.md §defaults` → see [Glossary → "default slot (per type)"](./ARCHITECTURE.md#default-slot-per-type)
  (exactly one `default = true` slot per type).
- `CONTEXT.md: gpu-rocm` → see [Glossary → "device"](./ARCHITECTURE.md#device)
  (`gpu-rocm` is the recommended default on Strix Halo; `gpu-vulkan` is the
  fallback).
