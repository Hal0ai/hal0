diff --git a/src/llama-vocab.cpp b/src/llama-vocab.cpp
index 6bffcbd..6dea846 100644
--- a/src/llama-vocab.cpp
+++ b/src/llama-vocab.cpp
@@ -2101,6 +2101,14 @@ void llama_vocab::impl::load(llama_model_loader & ml, const LLM_KV & kv) {
                 pre_type = LLAMA_VOCAB_PRE_TYPE_DEFAULT;
             } else if (tokenizer_pre == "default") {
                 pre_type = LLAMA_VOCAB_PRE_TYPE_DEFAULT;
+            } else if (tokenizer_pre == "minicpm5") {
+                // hal0: the enum (LLAMA_VOCAB_PRE_TYPE_MINICPM5) and its
+                // tokenizer behaviour already exist in this tree; only the
+                // name->enum mapping was missing, so every minicpm5-pretokenised
+                // GGUF (the shipped hal0-brain-sft family) failed to load with
+                // "unknown pre-tokenizer type: minicpm5".
+                pre_type = LLAMA_VOCAB_PRE_TYPE_MINICPM5;
+                ignore_merges = true;
             } else if (
                     tokenizer_pre == "llama3"   ||
                     tokenizer_pre == "llama-v3" ||
