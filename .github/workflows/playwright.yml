name: Playwright (γ)

# Critical-path UI tests — PLAN §10.3, ~8 min budget.
# Runs against the Vite dev server with all /api/* endpoints
# mocked via page.route. Live-backend runs (HAL0_E2E_LIVE=1) are
# release-gate only and live in release-test.yml, not here.

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded PR runs (the ~8 min γ-suite is the priciest PR check);
# never cancel a push to main — it's a required check + nightly gate input.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  e2e:
    name: γ-suite (chromium)
    runs-on: ubuntu-latest
    # Runner variance is extreme: the same 658-spec suite has completed green
    # in 7m24s and been cancelled mid-run at 18m19s on this very PR (see
    # #1926 — a 12-minute cap cost 5 reruns across #1913/#1934/#1942, and an
    # 18-minute trial cap was itself hit by a slow runner). 25 bounds a
    # genuinely wedged run; sharding is the real fix if variance grows.
    timeout-minutes: 25
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node 22
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install dependencies
        run: npm ci

      # Cache the Playwright browser binaries by package-lock hash so we
      # avoid the ~250 MB Chromium download on every PR.
      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('ui/package-lock.json') }}

      - name: Install Chromium (only on cache miss)
        if: steps.pw-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium

      - name: Install system deps (always — needed even when browser is cached)
        if: steps.pw-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium

      - name: Run γ tests
        run: npm run test:e2e
        env:
          CI: "true"

      - name: Upload Playwright report (failures only)
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: ui/playwright-report/
          retention-days: 7

      - name: Upload test-results (failures only)
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: ui/test-results/
          retention-days: 7
