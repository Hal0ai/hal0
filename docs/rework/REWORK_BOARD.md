# hal0 REWORK — canonical board (single source of truth)

> **How to use this board:** see `REWORK_BOARD_PROTOCOL.md` (paste-in prompt — reading order,
> single-writer rule, lane lifecycle, verify/merge/checkpoint discipline, agent-dispatch skeleton).

> **RATIFIED canonical board.** Replaces the dual trackers
> (`hal0-rework-tracker.md` = session-3/model-layer, `hal0-rework-tracker-surface.md` =
> session-4/surface+deploy). Once ratified, **retire both** — one writer-owned board only (review #10).
> Canonical spec = `REWORK.md` (finish line, checkpoints R1-R5, golden-paths, per-lane DoD; formerly
> `hal0-rework-plan-copy.md`). Full history/derivations = `hal0-rework-plan.md`. Latest handoffs:
> `hal0-rework-handoff-session3.md` (model-layer) + `/tmp/hal0-rework-handoff-session4-continue.md`
> (§7.4 privilege-drop pickup). This board is the *status* layer over the spec.
>
> Reconciled 2026-07-18 from both trackers + live session state. Base:
> `main` = `rework/descar` = `6aa565b8` (`rework-R2.1`); clean single checkout, zero divergence,
> no active agents. Scar 202 / baseline 202.
>
> **LIVE BOARD (2026-07-18, R4-primary remote session): this in-repo copy is now the live canonical
> board.** Sole orchestrator = the remote R4-primary session (single-writer rule applies to it);
> board updates ride merge pushes on `rework/descar`. Host-side `/home/mint/REWORK_BOARD.md` and
> `/tmp` handoffs are retired as authority — handoffs live in `docs/rework/`.

## Legend
`status`: ✔ done+CI-green · ▶ in-flight · ☐ todo · ⏸ deferred (safe, non-breaking) · ⛔ blocked
`owner_class` (collision class — serialize within a class): SEC · MODEL · RUNNER · SLOT · INSTALL ·
HERMES · API · UI · OBS · DOCS · DEPLOY
`checkpoint`: R1 secure+installable · R2 model-layer · R3 slot-runtime · R4 brain+hermes · R5 surface+launch

## Checkpoint status (the finish-line view)
| Ckpt | Theme | State | What remains |
|------|-------|-------|--------------|
| **R1** | Secure + installable | **✅ ON MAIN** (`ecdc0950`, tag `rework-R1`) | Merged 2026-07-18. Only tail open: KB-1 hardening (WS/SSE key audit, Security UI page, login rate-limit) — non-blocking. |
| **R2/R2.1** | Model layer + stabilization | **✅ ON MAIN** (`be4b0bba` / `6aa565b8`, tags `rework-R2` / `rework-R2.1`) | R2 model/runtime/docs/Hermes substrate plus R2.1 store-GC #8/#9 and vetted Hermes contract-pin foundation; main and descar have zero divergence. |
| **R3** | Slot runtime | **✅ ON MAIN** (`ab3e88f3`, tag `rework-R3`, collapsed 2026-07-18 from descar `671ca623`, CI run 29656507947 all-green) | Complete: slot-id identity + PortAuthority + dict[int] re-key + inert M5 migrator + quoting fix + Quadlet units + deep interface + GTT. Held for halo143 window: quadlet `@`-name verify, M5 live rehearsal, runtime id-flip. |
| **R4** | Brain + Hermes | **✅ ON MAIN** (`c91d0cf5`, checkpoint `rework-R4-stage` collapsed 2026-07-19 from descar `70bdcf2e`, CI 29666588999 + γ all-green; tag push blocked by git proxy — user pushes tag via thinMint, ref = `c91d0cf5`) | Complete: provision inc 1+2 (linear convergent `install_hermes`, scar 202→200), KB-4/5/6 board + KB-2/3 spec + shipped read-only steward, HP-memory/HP-provider plugins, HP-executor (inert at KB-5 seam), FLAGS-own inc 1, uniform quadlet render (O11), O12 rootful seam, UI D1–D6. Open tails: brain-phase relocation (5 RELOCATE markers), HP-executor live worker-path validation, host-net renderer lane, post-core HP-voice/automation. |
| **R5** | Surface + launch | **open** | Settings data-seam (MVP split done), §21.4 doctor ✔, docs collapse (P4-docs), migration rehearsal, cutover plan. Phase 3 waves 1–3 merged (`0a0bc6e8`): mem→hindsight, brain-lane→lifespan, drift-watch+runbook, SWEEP dead-code (w1); reconcile-wire, sunset-ratchet, persona-flag (w2); **FLAGS-own §2 centerpiece + migrator + v1.0.0 re-target (w3)**. Release pinned **v1.0.0**. Remaining: FLAGS-own §7 field-removal (deferred), typed-bodies (audit banked), MCP-autogen (needs §4.4 addendum), live install-validation (halo150/143, armed), descar→main promotion, Phase-5 version-string bump. |

**R1 → main is the highest-ROI next move** once the in-flight lanes reach a freeze point (review #3).

---

## Lane table

### R1 — Secure + installable  (land to main first)
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| P0-* | consolidate / descar / sunset guardrail | ✔ | — | — | descar base, `01c9a01a` | baseline 216 green | — |
| P1-* | pure-deletion de-scar (cognee, updater-a, agents, providers, shims, docs) | ✔ | — | — | batch1 `d7b2fa36`/`4eb9376f`+ | scar→215 | — |
| P2-toolloop | one tool-loop engine | ✔ | API | — | P2 safe-batch | CI-green | — |
| P2-hf | one HF client | ✔ | API | — | P2 safe-batch | CI-green | — |
| P2-composite | de-pseudo hal0 composite upstream | ✔ | API | — | P2 safe-batch `1d7e8721` | CI-green (fixed sig-drift) | — |
| P2-device | `device` sole truth, drop 4 translators | ✔ | MODEL | — | descar | scar 214→204, 786+ tests | — |
| KB-1 | gate board/chat/events + exposure table + middleware | ✔ | SEC | — | `4a868895` | 35 auth + exposure-CI green | — |
| KB-1-tail | WS/SSE `?api_key` audit · origin-check DiD · login rate-limit (Security UI page split to later UI wave) | ✔ | SEC | KB-1 | `5f5eb913` (was `89063bb8`) | CI+γ green on descar; uvicorn.error WS log scrub + origin gate before tier-auth + sliding-window login 429; scar 202 | — |
| P3-perms | one `hal0` user, OwnershipStore born-owned, `hal0-systemctl` | ✔ | INSTALL | — | descar | validated on halo (`doctor perms` clean) | born-owned OK on fresh |
| §17.7 | remove installer Honcho path | ✔ | INSTALL | — | `630173c6` | grep-clean, services cleaned (`b60a6614`) | — |
| §17.8 | minimal setup wizard (delete setup_ui.py) | ✔ | INSTALL/CLI | — | `2f8e10c9` | 9 cli tests | — |
| installer-fixes | NFS-tolerant comfyui-share · self-aware :3001 port check | ✔ | INSTALL | — | `51c542fa`,`51c36fab` | preflight 6/6; **halo install EXIT=0** | idempotent on halo |
| §21.7 | managed-arg denylist | ✔ | API | — | descar | CI-green | shipped seeds clean |
| §21.14/.15/.12/.5 | hal0 chat · PR-template · client-docs · /v1/models ext | ✔ | API/DOCS | — | `77e41b93`+ | CI-green | — |
| **#9 wheel** | wheel ships migrations+data | ✔ | DEPLOY | — | wheel-verify2 | ships 3/3 mig + 4/4 toml; `uv build` | verified |
| **#10 perms live-migration** | chown lxc105 root→hal0 | ⏸ | DEPLOY | — | — | MOOT for fresh halo | only for old lxc105 (not touching) |
| **#11 live-config strip** | drop drifted denied flags | ⏸ | DEPLOY | — | — | MOOT for fresh halo | old lxc105 only |

### R2 — Model layer
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| ML-1 | SQLite registry + files/models/revision tables | ✔ | MODEL | SQLite | descar `001_registry.sql` | 283 db+registry tests | — |
| ML-2/3 (ml-store) | file-set pulling + unified store resolver + by-id + refcount GC + `003_store` | ✔ | MODEL | ML-1 | descar `38f9d9e3` | 1490+ tests, scar→202 | migrations ship in wheel |
| ML-6 (§7.1d) | modality/capability taxonomy; kill tags→labels routing | ✔ | MODEL | — | descar | CI-green | — |
| **ML-4** | runner-image registry (`RUNNER_IMAGES`) + unified image resolution + `preferred_runner` | ✔ | RUNNER | ml-store | merge `4b7e9cb6` | merged suite; CI-green before later descar merges | — |
| **ML-5** | flag resolution (mtp/jinja capability, kill nojinja clones, family→arch) | ✔ | RUNNER | ML-4 | through `0852edfd` | merged suite; CI-green before later descar merges | — |
| R2-golden | store GC + pull/revision/refcount + multi-shard/mmproj golden-path tests | ✔ | MODEL | ml-store | `b7461bbe`; checkpoint `be4b0bba` | CI-green and merged to main | R2 accepted |

### R3 — Slot runtime
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| P3-slots | split manager.py (capacity/drift/npu/reaper) +§21.10 GTT | ✔ | SLOT | — | `dbc2c771` (4145→2769) | 392 tests | — |
| §11.1 | stable opaque slot-id; name = display label | ✔ | SLOT | — | merge `5d134c3b` | substrate tests merged | inert/additive on halo143 |
| §11.2 | PortAuthority (`port_claim` SQLite) | ✔ | SLOT | SQLite | `639cfb60`, merge `5d134c3b` | migration 004 + allocator tests merged | inert/additive on halo143 |
| **SLOT re-key (PR3–6) — increment A** | slot-id identity + PortAuthority wired live (acquire/release + TOML writeback, drop port=8081); /rename + /by-id + /by-name + /api/ports 5th source; non-destructive `fold_identity()` boot-fold | ✔ | SLOT | §11.1✔,§11.2✔ | `e0fd6d7c` (was `1f062b89`) | CI+γ green; additive/bijective (name↔id), 9 new + 668 targeted, scar 202; no exposure/migration change | — |
| **SLOT re-key — increment B** | internal `dict[int]` re-key of 7 dicts (single `_key` chokepoint, surrogate→rebind, rename = pure relabel) + M5 one-shot name→id artefact migrator (`migrate_id_keying.py`, idempotent, ships INERT) | ✔ | SLOT | increment A✔ | merge `91b55ad0` (lane `cd5e091b`/`3a4c9011`/`69c14bfc`) | Opus-built (TDD), Fable-reviewed + independently re-run: 550 combined incl. golden-paths harness; ruff/format/import/sunset green; 3 cross-fence one-liner test touches reported | **held for deploy (halo143 migration window):** live unit `hal0-slot@<name>`→`@<id>` + podman rename, M5 on real state, runtime path/unit flip to id (`_state_file`/`_config_file` + unit rendering) — must land atomically with M5 going live; consumed by P3-runtime-db |
| **container.py arg-quoting bug** | ExecStart emitted space-less tokens bare → systemd stripped inner double quotes → `{enable_thinking:false}` → llama JSON parse error → slot won't start | ✔ | SLOT | — | `cd5e091b` (in SLOT-B merge `91b55ad0`) | `shlex.quote` every token (safe tokens stay bare); regression test with the exact lxc105 token | unblocks no-think default for the brain (lxc105 §6) |
| SlotManager-deepen | `inspect/apply(desired)/delete/subscribe` small interface (review #4) | ✔ | SLOT | §11.1✔ | merge `919b2f36` (lane `b47c039c`) | Opus-built, Fable-reviewed + independently re-run (466 combined); id-keyed SlotInterface facade via `SlotManager.interface`; additive-only (wide surface intact); DesiredSlotState documents FLAGS-own narrowing; 12 new interface tests | wide-surface collapse = later increment (after FLAGS-own) |
| P3-quadlet | Podman Quadlet `.container` units; hand-rendered ExecStart chain + docker fallback DELETED; instance token templated (`slots/naming.py` — M5 id-flip = 1 parameter); privilege seam `write-quadlet`/`remove-quadlet` (validated) | ✔ | INSTALL/SLOT | P3-perms✔, §11.1✔ | merge `ad821f9d` (lane `00843bfd`) | Opus-built, Fable-reviewed + independently re-run (554 lane + 504 combined incl. golden-paths); quoting regression ported; seam.py additive-only | **VALIDATED on halo150 (2026-07-18):** `@`-named `.container` accepted, generator converts (via podman-4.x `PodmanArgs=` compat `5adf6e0f` — AutoRemove/GroupAdd/SecurityOpt are 5.0-only keys; GPU groups preserved), container Up + health + GPU inference + clean teardown on podman 4.9.3 (= lxc105's substrate). Held: OwnershipStore row for /etc/containers/systemd. Q.3 OpenWebUI companion deferred. Podman-5 template refresh = R5 DEPLOY row (native keys + crash-path auto-remove) |
| §20 bench | bench_run onto OBS schema | ☐ | OBS | ML-4 | spec `spec-bench.final.md` | — | needs GPU box; **note: hal0 llama.cpp forks reject newer GGUFs (e.g. Qwen3.5 UD-Q4_K_XL) on `rocmfp4-server`/`c077206`** — bench only models hal0 already serves (lxc105 §5.4) |

### R4 — Brain + Hermes
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| P3-brain | first-class `src/hal0/brain/`, zero-Hermes-dep, `/api/brain/chat` primary + `/api/board/chat` alias | ✔ | HERMES | P2-toolloop✔ | merge `24502baa` (was banked `b66b5e1e`, rebased onto `eede41c1`) | rebased+re-verified by orchestrator: 172 targeted (brain/board/exposure/route-collision combined), ruff+format, import smoke, scar 202; independent Fable review approved (sys.modules alias, ADMIN exposure rule, zero-Hermes-import verified); CI pending on merged tip | **brain model assets exist** (lxc105): `hal0-brain-sft` f16 + `-fpx4`(609MB)/`-fpx8`(1.1GB) ROCmFP4 quants registered, profile `brain-agent-fpx` — see `hal0-105-changes-summary.md`. No-think default needs the container.py quoting fix OR a `minicpm5-nothink.jinja` (internal `enable_thinking=false`). Brain identity correct only with hal0-brain system prompt (Hermes supplies persona) |
| §7.4 hermes-slim | privilege-drop (born-owned) → F.7 chown deletion; convergent installer slimming continues separately | ✔ | HERMES/INSTALL | P3-perms✔ | merge `86283e94` | incs 1-5 + F.7 reviewed; born-owned halo143 validation | deployed/validated on halo143 |
| HP-compat | select and test reviewed official Hermes tag/commit against the three plugin contracts | ✔ | HERMES | §7.4✔ | `f3e4e3e6`, guard fix `6f844901`, checkpoint `6aa565b8` | independent review; 79 targeted tests; full CI + Playwright green after vetted-ref guard reconciliation | R2.1 on main; Stage 0 accepted |
| HP-core | shared Hermes adapter core: auth, discovery, typed errors, retry policy, correlation IDs | ✔ | HERMES | HP-compat✔, KB-1✔ | merge `b1115b9d` | independent review + security re-review approved; 61 combined tests; exact-head full CI + Playwright green | merged on descar; Stage 1 core accepted |
| HP-role-api | generation-stamped runtime `GET /api/agents/{agent_id}/role-slots` + invalidation events | ⏸ | API/HERMES | §11.1✔, §11.2✔, KB-1✔ | helper `src/hal0/agents/role_slots.py:152`; WIP scaffolding banked `c11b21cb` | **row corrected 2026-07-26** — the prior `▶` claimed the REST route was merged on descar. It was not: no `role-slots` route exists in `src/hal0/api/routes/agents.py` on `main`, and `git log --all -S 'role-slots' -- src/hal0/api` finds only the explicitly-WIP `c11b21cb` ("banked … handed to primary Hermes orchestrator"). **Not dropped by the #1353 collapse — never built.** | **Capability is NOT missing:** `resolve_role_slots()` ships and is consumed by `hermes_provision.py:3849`, and role-slot *addressing* has shipped since #430/#431 via the `/v1` alias table (`api/routes/v1.py:297,747`). Only the generation-stamped REST surface + invalidation events are unbuilt. Deferred post-1.0 — no user-visible gap. |
| HP-contract-surface | drift-watch → official pin (NousResearch `9de9c25f`) + contract freeze expanded 6→18 tracked files across all adapter touchpoints (MemoryProvider ABC, ProviderProfile, voice/PluginContext, API-server routes + security defaults) | ✔ | HERMES | HP-compat✔ | merge on descar (`merge(hermes): contract freeze expanded`) | 24 tests + 1 strict xfail; Opus-built, Fable-reviewed + independently re-run post-rebase; fixtures verbatim from pinned source; design-doc mismatches adjudicated (events.py dropped — hal0-side EventBus; runtime.py unfrozen — no stable single-file seam at pin) | **DECIDED (user, 2026-07-18): accept `terminal.backend=local`, harden the seam hal0 owns** — (i) audit + pin unit hardening (NoNewPrivileges, ProtectSystem=strict, ProtectHome, minimal ReadWritePaths, no hal0-secret paths) with a CI-checked test on the rendered unit; (ii) move `terminal.cwd` off `/etc/hal0` to a scratch dir; both fold into the hermes-provision rewrite lane. Strict xfail stays as the upstream-default record |
| hermes-provision inc 1 | six security/validation deliverables: unit-hardening CI test · terminal.cwd→scratch · strong random API_SERVER_KEY · AppArmor containers.conf preflight (smoke-triggered) · stale agent drop-in cleanup (241 class) · --repair ownership reconcile | ✔ | HERMES/INSTALL | §7.4✔, HP-contract-surface✔ | merge `db3c5513` (lane `0703a0ec`) | Opus-built, Fable-reviewed + independently re-run (71 targeted + 149 combined); all six with tests; contract suite green | deploy smoke on 150 (apparmor path) + 143 (clean path) per both-boxes policy |
| hermes-provision inc 2 | the convergence rewrite: PHASES/run()/provision.json machinery + adopt/foreign-gateway/wrapper-capture paths DELETED → linear convergent `install_hermes(repair=False)`; 5,696→5,065 lines; scar 202→200 (baseline lowered) | ✔ | HERMES/INSTALL | inc 1✔ | merge `94ebea5d` (lane `4dfbc11b`/`c22ea608`) | Opus-built, Fable-reviewed; double-run test = run 2 mutates NOTHING (config byte-identical); rewritten trio hermetic (vault/paths pinned to tmp); callers kept via thin shims; hal0-provider plugin_targets entry wired (merged-tree assertion flipped to actually-copies); 585 tests/agents + 115 CLI-caller green | brain-phase relocation still in-place, 5 `RELOCATE(brain-lane)` markers; module split deferred (deletion beat splitting) |
| HP-memory | exclusive `hal0-memory`: private raw turns, shared durable facts default, private durable override, ranked provenance recall | ✔ | HERMES | HP-core✔, HP-contract-surface✔ | merge `b010c7ec` (lane 2 commits) | Opus-built, Fable-reviewed; full 19-method roster; shared-default flip (was inverted private-default); 5 privacy + 4 injection-resistance tests; two-copy parity byte-verified; 141 passed + ratified terminal.backend xfail | queue_prefetch = single-slot park (no in-plugin worker, honest bound) |
| HP-provider | `hal0-provider` (`chat_completions`): live slot/model inventory and restart-free role aliases | ✔ | HERMES | HP-core, HP-role-api | merge `57461cbe` (lane `974b7609`) | Opus-built, Fable-reviewed; contract-fixture-driven registration (module-level `register_provider` per frozen 9de9c25f — spec §4's ctx seam doesn't exist); no-cache fetch_models = hot-swap; 57 lane tests, 174 combined green | plugin_targets dir-drop entry rides provision-inc2 (`"hal0-provider": …/model-providers/hal0`) |
| HP-voice | `hal0-voice`: Hermes STT/TTS routed through existing hal0 voice slots | ⏸ | HERMES | HP-core, §11.1✔, §11.2✔ | design `d75e5f88` (local) | capability/readiness, audio limits, interruption/fallback tests | **post-core (user 2026-07-18)** — below the R4 exit bar |
| HP-executor | hal0-board → Hermes worker bridge; hal0 remains canonical | ✔ | HERMES | HP-core, KB-4/5/6✔ | merge (lane commit `d41e1f8a`) | Opus-built, Fable-reviewed; attaches at KB-5 seam via register(); inert unless HERMES_DASHBOARD_BASE_URL set; 26 tests incl. spy-store invariant (zero canonical mutators called); 82 board tests green | ⚠ WORKER_BASE_PATH `/api/plugins/kanban/runs` is unpinned by contract fixtures — validate against live Hermes at next both-boxes deploy |
| HP-realtime | inc-1 ✔ MERGED: `WS /v1/realtime` — server_vad (energy-RMS in-process, ZERO new deps, config-tunable, silero-swappable seam) + turn_detection:none; full accept/reject event vocab (GA `response.output_audio.delta` naming, matches demo); BOTH legs (steward via brain SSE — zero chat.py edits — w/ spoken approval notice + 20s bounded wait vs 300s dead air; generic /v1 + client tools); pcm16@24k out, WAV-wrap STT in; CLIENT exposure row; `docs/guides/realtime-voice.mdx` w/ verified mcp-remote recipe (no mount change needed) | ▶→✔ | API/SLOT | spec e3974a78✔, 3 user decisions | merged (lane 2 commits) | Opus-built, Fable-reviewed; 79 lane + 104 combined green; found+fixed slice_pcm_frames odd-byte bug | inc-2: silero VAD, streaming ASR via existing moonshine toolbox WS + /v1 proxy, vibevoice toolbox server (none exists — currently kokoro-mapped) |
| HP-automation | Hermes Jobs/cron for scheduled agent work using stable hal0 role aliases | ⏸ | HERMES | HP-core, HP-provider | plan `447a851f`+ | CRUD/lifecycle, alias hot-swap, cron-memory isolation, no maintenance jobs | **post-core (user 2026-07-18)** — below the R4 exit bar |
| HP-context | optional non-memory context-engine plugin | ⏸ | HERMES | HP-core | design `d75e5f88` (local) | separate context contract suite if promoted | post-core; do not merge with memory |
| KB-2/3 | brain read-only default + approval-gate; resilience | ✔ | HERMES | — | merge (lane `f734dab0`/`57eca98f`/`d64577cc` + orchestrator `2758eb53`) | Opus-built, Fable-reviewed; spec-kb23-brain-tools.md (full tier table: every surfaced tool classified, approval state machine, injection posture); 51 lane tests (37 read-only, 7 injection, 7 resilience); read_only default flipped TRUE by orchestrator at merge (§4b cross-lane reconciliation — 3 board harnesses opt in, 261 brain+board green) | downstream contract for HP-executor/HP-automation now pinned |
| KB-4/5/6 | hal0-owned board in SQLite (migration 005); Hermes proxy DELETED; local WS events; additive ETag/If-Match → 409; KB-5 executor seam (Protocol + no-op registry) | ✔ | HERMES | SQLite✔ | merge `516965d9` (lane 3 commits) | Opus-built, Fable-reviewed + independently re-run (240 lane + 283 combined); frozen wire contract; one-time tolerant import; board serves with hermes_kanban=None (core-without-Hermes) | HP-executor now unblocked (attach at the KB-5 seam) |
| HP-legacy-suite | superseded image/TTS/context/dashboard plugin bundle | ⏸ | HERMES | — | history/specs banked | superseded by focused HP-* lanes | post-core unless explicitly promoted |
| O12-store | rootful/rootless store split: hal0-api podman introspection now via `sudo -n hal0-podman-ro` seam (privileged seam #5, `images` verb only, hardcoded argv); honest rootless fallback + `podman_context` field on system-info; 9e07c0d3 `.config`/`.local` chown rows RETIRED (lock rows kept) | ✔ | DEPLOY/INSTALL | halo143/150 finding | merge `252e860c` (lane `cbc8e94d`) | Sonnet-built, Fable-reviewed; 116 lane + 56 combined green; visudo/bash -n clean; mcp.py had no podman site (brief assumption corrected) | **LIVE-VALIDATED both boxes 2026-07-19** (r4-stage-validation.md): podman_context=rootful, installable→installed |
| steward-auth (O17/O18) | steward dead on auth-on boxes: internal /v1 POST carries no bearer (401) + brain-chat framing trips template user-query guard (500 `multi_step_tool`) | ✔ | BRAIN | R4-stage validation | merged (lane 2 commits) | Opus-built, Fable-reviewed; THREE self-call sites fixed (v1=CLIENT tier, platform=ADMIN, admin-dispatch fallback added); forward-caller-bearer first, service identity fallback (new shared `service_identity.py`, CLI delegates — O2 surfaces can't drift); O18 root cause = singular `{"message":…}` payload silently dropped → zero-user completion; `_frame_messages` guarantees ≥1 user turn all rounds; 150 passed (19 new wire-shape/auth tests) | 150's local patch superseded at next redeploy; dashboard auths via cookie so service-identity fallback IS the fix path |
| install-fixes (O13–O16) | fresh-install slots tree root-owned + perms --fix gap; `--adopt` flag not retired; uv-fallback HOME=/root leak (py3.14 hosts); uninstall 500 leaves HERMES_HOME | ✔ | INSTALL/HERMES | R4-stage validation | merged (lane 4 commits) + orchestrator `be46ecc9` (venv-refresh delta applied) | Opus-built, Fable-reviewed; O13 both-ends (birth chown 2775 + slots/+registry/ PermRows → --fix self-heals); O14 flag gone (unknown-flag on --adopt); O15 `_hal0_subprocess_env()` HOME hygiene on ALL post-drop subprocesses; O16 theory CONFIRMED (unguarded rmtree as hal0) → `_rmtree_collecting` + typed 207 MultiStatus w/ residuals, runtime.json chown leak fixed; 213 lane + 182 combined green | adopt stragglers in docs/reference/cli.mdx → docs lane; O13–O18 ALL fixed on descar — steward stack redeployable once CI green |
| login-ui (O19+) | AuthGate + LoginView (masked key, 429/wrong-key states, no key persistence); auth OFF by default — `require_auth_enabled()` now env → persisted `[security].require_auth` → OFF (bind/key auto-on RETIRED); Security page real: live PUT /api/auth/require toggle (enable refuses w/o admin key → no lockout), POST /api/auth/logout (both exposure-classified); WS/SSE ride the cookie (no client change needed) | ✔ | UI/SEC | O19, user directive | merged (lane 4 commits) | Opus-built, Fable-reviewed; auth-core re-pinned to inverted posture; 56 auth/exposure + 100 combined green; ui build + tsc clean; 8 new e2e tests | key ROTATION still env-only (api.env) — reported not built, RotateKeyDialog stays gated-with-reason; login = admin-key-only by backend design |
| perms-recurse (O13b) | recursive opt-in on PermRow (`recursive`+`child_file_mode`, default-off = zero behavior change to existing rows); slots/ row recursive w/ dirs 2775 + files 0600 (matched write_state_atomic's mkstemp birth mode, not the brief's 0664 guess); 3 explicit registry/ rows matched to each writer | ✔ | INSTALL | O13 | merged (lane `41e92595`) | Sonnet-built, Fable-reviewed; 24 perms tests incl. depth-2 drift+heal repro and non-recursive-row regressions | — |
| steward-nomodel | unrouteable brain model → actionable SSE error (names tried model + load-slot/set-model fix + 8k-ctx floor); empty model short-circuits (no wasted self-call); transport/non-404 errors stay distinct; ctx-floor documented in BrainChatConfig | ✔ | BRAIN | validation note | merged (lane `fd55042d`) | Sonnet-built, Fable-reviewed; 9 new + 138 combined green | lane REFUSED the mid-build O21 doc update as suspected injection (verified DEFAULT_CHAINS contradicts the live finding — the discrepancy IS O21's investigation scope) |
| default-consumers | per-type model default now CONSUMED: `fallback_local_model` step-0 prefers the type's default among servable candidates (unset/unservable → unchanged heuristic); create modal preselects the type default when no explicit model (explicit always wins) | ✔ | MODEL/UI | model-default-type✔ | merged (lane 2 commits) | Sonnet-built, Fable-reviewed; 37 py + 6 e2e green; found+documented mock-allowlist intercept gotcha (HAL0_DATA setter idiom, not page.route) | — |
| docs-stragglers | user-facing docs: --adopt/turnstone rows deleted, primary/chat examples → current names (source-verified); CLI --help wording fixed at SOURCE (state-machine/checkpoint → convergent pass) | ✔ | DOCS/CLI | provision inc-2✔ | merge `be9f1cef` + `d31dd32f` | Sonnet-built, Fable-reviewed; 115 CLI tests green | setup-wizard docs rewrite + npu/flm naming → P4-docs |
| small-fixes (O25+) | LOCAL-SESSION LANE: renderer mounts pull_root + store (deduped) via `model_mount_roots()` + `doctor models` HAL0-MODEL-STORE-UNMOUNTED guard (O25 real fix — retires the per-box store stopgap); type-less llm-shaped slots auto-heal to type="llm" on load (O23 residual); steward prompt-vs-ctx pre-flight (actionable SSE error before the guaranteed 400); ctx-drift already covered → regression-locked; image-status ref-match already fixed → confirmed | ✔ | SLOT/BRAIN/INSTALL | O25/O23/ctx findings | merge `9c898dd5` (operator local session, Fable-coordinated) | 194 lane tests + ruff/import/sunset green | boxes on c5ee539d + stopgap — next redeploy drops the hand-edit |
| key-rotation | LOCAL-SESSION LANE: `POST /api/auth/rotate` — real key rotation, status-only responses (never key values), atomic 0640 api.env write, live no-restart, ADMIN-gated; RotateKeyDialog wired | ✔ | SEC/UI | user "YES" 2026-07-19 | merge `c82abffe` (operator local session) | gates green | closes the login-ui lane's reported rotation gap |
| post-wave fixes | C7d RETIRED (real bug: flmModels non-array payload crashed the slots view — Array.isArray guard); O24 closed (ui-dist tree-hash stamp, ef240edd); SecurityConfig extra=forbid + policy-lock entry; AuthGate/approvals-spec mount race; version-flash placeholder; drawer default badge live-update; same-version venv force-refresh in install.sh | ✔ | UI/CI | γ/python reds on #1312 | fixes `618ab5e2`…`dd1ef791` | CI + γ ALL GREEN on dd1ef791 (451+ e2e, 6642 python) | descar COLLAPSE-READY — awaiting user go |
| O21 virtual-id routing | ROOT-CAUSED + FIXED (orchestrator, 70c7fa62): NOT an advertising gap — `_normalize_loaded_models` filtered upstreams kind=="remote" but SlotManager registers local slots kind="slot" → resolver chains starved of every local slot's model → ALL hal0/<slot> aliases 404'd on real boxes regardless of slot state; collector now keys on the container-backed `slot_name` marker | ✔ | API/MODEL | operator finding + live 404 evidence | fix `70c7fa62` | 2 regression tests incl. end-to-end hal0/brain rewrite with the exact kind="slot" shape a live box registers; 464 slots+normalization green | `[brain_chat] model="hal0/brain"` works at next redeploy; stale kind=remote docstrings in manager.py corrected |
| O20 kanban schema | ROOT-CAUSED (operator) + FIXED: new convergent `kanban_db_init` provision step invokes the HERMES venv's own `hermes_cli.kanban_db.init_db` when sqlite_master lacks any of the 7 tables (hal0 reads, Hermes creates — zero pin drift); venv-absent → honest SKIP | ✔ | HERMES/INSTALL | operator root-cause 2026-07-19 | merged (lane `6a683dc7`) | Sonnet-built, Fable-reviewed; 6 step tests + double-run convergence intact (127 combined) | operator already force-inited both live boxes; step covers all future installs |
| model-default-type | per-type model default on `Model.default` (extra-blob persisted, no migration); `set_model_type_default` chokepoint (promote demotes holder, self-heals doubles); `POST /api/models/{id}/default` (ADMIN exposure row); slot-create promotes fail-soft (`default_promotion` on response); drawer badge + set/remove | ✔ | FLAGS/UI | user request 2026-07-19 | merged (lane 2 commits) | Opus-built, Fable-reviewed; 17 lane + 133 combined green; tsc/eslint clean; 2 new e2e specs | KEY: checkbox fed the SC-4 SLOT-level default before — model-level default was the missing axis (SC-4 untouched); consumers reported not wired: registry/fallback.py `fallback_local_model` + modal preselect |
| hostnet-render | operator-validated 143 fix: paired `Network=host` + loopback bind fence at ONE renderer chokepoint (`_loopback_fence_command` — split/inline/shell-payload shapes, `--host` + `--listen`); new `[slots].network_mode` knob (""/host, config-driven, NO probing) | ✔ | SLOT/DEPLOY | podman-unprivileged-findings✔ | merge `bf05310d` (lane `aa46045e`) | Opus-built, Fable-reviewed; invariant test: host-net plan can NEVER render 0.0.0.0; bridge regression pinned; all 5 bind-templating backends covered; found+closed pre-existing ComfyUI pairing violation; 235 combined green | ⚠ ComfyUI web UI now loopback-only under host net (was LAN :8188) — user veto window; preview path (`resolved_command_for_slot`) unfenced (informational only) — follow-up; install.sh venv-refresh delta held for post-install-lane apply |

### R5 — Surface + launch
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| P3-ui | settings.jsx 2598 split → Shell + 16 pages (MVP) | ✔ | UI | KB-1✔,§7.1d✔ | `2a1d2290`, PW-fix `c175980d` | 420 γ pass | — |
| P3-ui-dataseam | one typed settings client + schema + reload-class source (review #8/§K) + land Backend + Model-Defaults pages | ✔ | UI | ML-4✔ | `0c93a1f3` (was `41018109`) | CI+γ green (C7d flake cleared on re-run; local full γ 422/0); settingsClient façade + reloadClass source (ApplyBadge amber-chip fix) + useSettingsForm; Backend/GPU + Model-Defaults pages; scar 202 | — |
| §21.4 | doctor command + §21.3 system-info | ✔ | CLI | — | merge `0fdde455` (lane `9fbc81d1`) | Sonnet-built, Fable-reviewed (redaction path audited: canonical redact_config + key-name env scrub + Bearer/JWT text scrub) + independently re-run (77 lane + 33 combined); Diagnosis taxonomy (frozen HAL0-* IDs), --json on all 5 subcommands, `doctor bundle`, GET /api/system-info (CLIENT rule pre-landed — no exposure edit); PR6 gfx-guard IDs reserved for SLOT lane | bundle probes (rocminfo/rocm-smi/podman/journalctl) + system-info installed-state held for halo143 |
| P3-routers (inc 1) | extract pull-jobs/metrics/image-pull services from the two mega-routers (§J) | ✔ | API | — | merge `42d45603` (lane 3 extraction commits) | Opus-built, Fable-reviewed + independently re-run (509 lane + 31 combined); models.py 1774→1298, slots.py 1964→1513; re-export bindings keep api.__init__/monkeypatch seams; paths/order/payloads frozen, exposure untouched, route-collision guard green | — |
| P3-routers (inc 2) | slots voices/logs/flm_catalog/port_alloc → services; benchmarks/chat_templates/comfyui typed errors (routes/ HTTPException = 0) + comfyui typed bodies; models add-from-path/HF-inspect/list_all → models_service | ✔ | API | inc 1✔ | merge `b9339d27` (5 lane commits) | Opus-built, Fable-reviewed + independently re-run (213 post-rebase); status codes byte-preserved (kept 422 over spec's 400 slip); exposure untouched; port_alloc.py docstring-flagged as PortAuthority merge target | models.py 987 / slots.py 1289 vs DoD ≤550/≤800 — gap = deferred pull-orchestration extraction (spec step 9) |
| P3-routers (inc 3) | remaining: pull/update-pull orchestration extraction (highest-risk, monkeypatch-heavy), models/slots typed bodies (needs dashboard-key audit — status-code collision), MCP admin route-map autogen (spec step 20, big standalone) | ✔ | API | inc 2✔ | spec §5 steps 9/16/19/20 | **all three closed (2026-07-20):** typed-bodies = PR #1322 (status-code-preserving pydantic bodies for models/slots command routes); MCP route-map autogen = PR #1323 (`build_admin_route_map(app)`, route-id keyed, deny-by-default, 82-alias table, 233 hidden); pull/update-pull = **no-op, already landed** `15b9d344` (row 177). DoD line targets still open: models.py 891 / slots.py 1396 vs ≤550/≤800 = further decomposition lane | — |
| route-collision-test | reject literal shadowed by param routes (review #5/§J) | ✔ | API | — | **pre-existing `43f29e30`** (row was stale) | already landed in the R3-A stack with version-proof `_IncludedRouter` flattening + positive control; the duplicate merged at `0b93a48b` failed CI on newer FastAPI (0 APIRoutes via lazy wrappers) and was removed (`fix: drop duplicate route-collision test`). Lesson: grep for an existing owner before dispatching a board row | — |
| golden-paths-early | CI-runnable §21.11 subset: #9 rename · #10 delete-cleanup · #14 api-restart-no-bounce · #15 core-without-Hermes | ✔ | DEPLOY/tests | — | merge on descar (`merge(golden)`) | 9 tests, interface-level only (public routes; survives SLOT-B internals rewrite); Opus-built, Fable-reviewed + independently re-run; deploy-only remainders documented per scenario in `tests/golden_paths/__init__.py` for the halo143 runbook | acceptance harness for SLOT increment B |
| FLAGS-own | flags stick to MODELS; profiles = copy-on-stamp templates; slots lose flag/device/template surface; kill `profile.image` pin; "Runtimes" panel for runner images | ✔ (v1.0.0 scope) | MODEL/UI | ML-4/5✔, SLOT-B (quoting fix), P3-ui-dataseam✔ | spec `spec-flags-ownership.md` (fully ratified 2026-07-18 incl. §7 slot-purity fold: device + chat_template → model; slot = id/name/model/port/state; create-slot device picker stamps the model) | migration folds slot overrides into models (divergent-share refusal path); golden #5 asserts no profile read at launch | **shipped:** §2 flag-text fold (task #9), UI D1-D3 (`2cfec5e1`), §7 `chat_template` fold PR #1325 (slot→model, divergent-share refusal, `HAL0-SUNSET: v1.0.0`; `SlotConfig.image` found already sunset-stamped). **Deferred post-1.0 (user 2026-07-20):** §7 `slot.device` removal (Model runner-axis + ~10 readers rewired, behavior-risk, needs live GPU validation) + `profile.image` delete (blocked on updater stale-pin hatch) → row 201 / task #23 |
| P4-docs | collapse ARCHITECTURE/CONTEXT/AGENTS → one; ADR-or-inline | ✔ | DOCS | — | merge `ecbdc6f6` (lane `81b6857d`) | content-collapse already landed (ARCHITECTURE.md = single authoritative doc, ADR-inline); finished it: deleted CONTEXT.md + AGENTS.md redirect stubs, retargeted 6 in-code CONTEXT.md doc-comment refs; root AGENTS.md had zero code dep (all src/test refs = unrelated /etc/hal0/AGENTS.md); ruff clean, 134 touched tests green | — |
| P4-tests (infra) | CI installs from uv.lock (setup-uv + `uv sync --frozen` — kills the CI-vs-local version-skew class) + podman/systemd/network markers registered & applied | ✔ | CI | — | merge `ff8817ce` (lane commits salvaged after agent stop) | Fable-reviewed + verified (yaml parse, marker collection smoke 1323/1325); CI behavior unchanged, local capped verify can now run tests/api with `-m "not podman and not systemd and not network"` | remaining: C7d flake diagnosis, CI tier split — fold into a later P4-tests increment |
| P4-rules | anti-scar rules in CONTRIBUTING | ✔ | DOCS | — | merge `fa9085d0` (lane `bba0f847`) | 9 gated anti-scar rules already present; added the two bit-twice CI lessons as rules 10-11 (test behavior not `app.routes` shape / CI from lockfile; find the owner before adding a parallel) | — |
| UI-D1-D3 | design-canvas implementation: ModelDrawer (copy-on-stamp flags editor + divergence + reset + managed-arg pre-check + duplicate flow), slot purity surfaces (create/rename/delete dialogs, slot-modals −337 lines), Runtimes page | ✔ | UI | FLAGS-own spec (ratified), design canvas | merge `2cfec5e1` (lane 3 phase commits) | Opus-built, Fable-reviewed + independently re-run (lint/typecheck/build 0; 23-spec targeted γ re-run incl. rewritten slot-drawer-profile-v3; agent full run 127 γ green); client managed-arg mirror carries keep-in-sync note vs argv.py | D4-D6 (Security/migration-UX/diagnostics panel) = follow-up UI lane |
| UI-API-1 | backend affordances the D1-D3 surfaces flagged: (1) model-save managed-arg screen or POST /api/models/{id}/validate — UNVERIFIED whether model PUT screens extra_args vs §21.7 denylist (AUDIT FIRST, possible live gap); (2) RUNNER_IMAGES exposure w/ format/arch compat + writable preferred_runner for the override dropdown; (3) POST /api/models/{id}/duplicate (refcounted weights + template stamp); (4) per-runner digest-drift + pull route (ADMIN, SSE) for Runtimes actions | ✔ | API/MODEL | UI-D1-D3✔ | — | **AUDIT DONE 2026-07-19 — item-1 gap CONFIRMED, worse than the guess:** (a) `POST /api/models` create has NO managed-arg validation (`routes/models.py:278-323`); only PUT validates (`:587`). (b) at launch the `model_defaults` segment is NOT in `UNTRUSTED_SEGMENT_LABELS` (`slots/argv.py:107` = only `"extra_args"`) → a denied flag (e.g. `--port`) in `defaults.extra_args` bypasses BOTH save AND launch, reaching the container. (c) no `/validate` endpoint; `ModelDefaults.extra_args` = bare `str\|None`, no validator (`registry/model.py:82-85`). Denylist = `MANAGED_ARGS_DENYLIST` `slots/argv.py:90-99` (--model/--ctx-size/--host/--port/-ngl/--alias). Fix = screen create-path + add `model_defaults` to untrusted labels (or validate at save). **BUILD = GitHub PR #1316** (this audit was its spec) — touches models.py/argv.py/container.py/services. **descar→main reconciliation:** rebase onto current `origin/main` (main advanced past descar's branch base: `462b28f` + #1315 merged + #1316 pending-green); this row points AT #1316, do NOT re-spec it as unbuilt. | ✔ PR #1316 merged → descar `4f828feb` |
| §21.11 golden-paths | the 15 deployment-shaped scenarios (plan-copy L602) | ✔ | DEPLOY | — | merge `fa9085d0` (lane `ecc36306`) | complete 15-scenario coverage map in `tests/golden_paths/__init__.py` — each scenario = one mechanism (CI-here #5/#9/#10/#14/#15 · existing-suite #4/#6/#7/#8/#11/#12 cited to auth/registry/perms/config suites, NOT duplicated per anti-scar rule 1 · deploy-runbook #1/#2/#3/#13); `docs/rework/golden-paths-halo143-runbook.md` scripts deploy-only halves (both-boxes policy); cited paths verified, golden suite 11 green | **pull earlier (review #7)** — subset #9/#10/#14/#15 landed CI-side (golden-paths-early row); remainder = existing-owner citations + halo143 runbook |

#### Phase 0 — correctness & security (Wave 0, §8) — LANDED 2026-07-19; γ fallout fixed + descar reconciled with `main` (#1317 `f2d82d38`); tip `64669944` (γ green, CI running). Drive-2 handoff canonical at `docs/rework/handoff-r5-drive2.md` (deduped with #1317's); SWEEP discovery done → `docs/rework/sweep-inventory.md`.
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| SEC-mcp-clientid | MCP raw-bearer stamped into journald as `client_id` (launch-blocker #2, §4.2) — hash the label, fix pinned test, add length-gated `client_id=` redactor | ✔ | SEC | — | merge `5d7cd283` (lane `54a19c85`) | `bearer_resolver` returns sha256[:12] label not raw key; `client_id_resolver` untouched; ruff+format clean, tests/mcp 151 pass; Opus-dispatched Sonnet lane + orchestrator diff-review | — |
| MCP-patch | `upstream_update` PATCH always crashed (`_call_rest` lacked PATCH) + no route-sync pin (§4.1) | ✔ | API | — | merge `b878ff9d` (lane `65fe49f1`) | `_call_rest` table-dispatch (verb→payload-kwarg), import-time `_validate_catalog` guard, new `test_route_sync` pins every `_REST_MAP` entry to a live route; PATCH route confirmed at providers.py; tests/mcp 153 pass | — |
| CLI-auth | 4 CLI transport sites bypass `_auth_headers` → 401/traceback on auth-on boxes (§5.1) + 2 help texts pointing at deprecated verbs | ✔ | SEC/CLI | — | merge `d55aa36d` (lane `1dedecd7`) | authed stream helper in `_shared` (auth_client/api_stream/follow_sse_logs); slot/doctor logs -f, chat, setup apply/probe routed through it; 401/403 actionable; tests/cli 546 pass (1 pre-existing rocminfo-env fail, confirmed on stash) | — |
| mock-prod-gate | prod mock fallback faked data + non-GET success (launch-blocker #3, §1.1) — was NOT pre-fixed; real gaps found | ✔ | UI | — | merge `54ccd580` (lane `268f96ac`) | substitution now needs `method==='GET'` for ALL rows + `FORCED\|\|DEV`; fixtures split to dynamically-imported `mockFixtures.ts` → vite build dead-code-eliminates them (verified in bundle); added vitest infra + 3 regression tests; tsc/eslint/build clean | — |
| INSTALL-target | ship `hal0.target` (launch-blocker #1, §6.1) + model-store PermRow (§6.2, O13) + uninstaller sync (§6.3) | ✔ | INSTALL | — | merge `1bb9bf70` (lane `b9c0b0e5`) | new `installer/systemd/hal0.target` + install/enable/uninstall wiring + `doctor` check; models/ PermRow 2775 setgid + O13 chown; uninstaller sweeps quadlet sources/sudoers/bench-purge/honcho units/hermes shim; bash -n clean, tests 637 pass (same rocminfo-env fail) | **LIVE VALIDATION DEFERRED — both boxes (150/143), Phase 4**: reboot-autostart, `doctor perms --fix` + default-store pull, upgrade-in-place, uninstall/reinstall no-ghost-slots |
| SEC-hermes-mcp-cred | provisioned MCP configs sent no credential → every Hermes/brain tool call 401s with auth on (§4.2) | ✔ | SEC/HERMES | — | merge `1c01dbab` (lane `23cb35c6`+`f1af296f`) | `service_key(prefer=admin)` threaded (fresh per call, dev-open unaffected) into `_build_config_overlay`, `_build_brain_profile_mcp_servers`, `_probe_mcp_server`, and `_mcp_memory_call` (both `/mcp/*` and ADMIN-classed `/api/memory/*`); golden-path boots real auth gate + negative control; 192 lane / 401 broader pass | rotation re-propagates via idempotent `agent bootstrap hermes --repair` |
| activitylog-γ-fix | mock-prod-gate fallout: lazy `import('./mockFixtures')` raced React first-commit, remounting `<ActivityLog/>` and reddening γ `activity-log.spec.ts:152` (bounded-scroll) | ✔ | UI | mock-prod-gate✔ | merge `979f58e5` + combined de-flake `abb6db2a` (both sessions) | eager mockFixtures warm (FORCED-gated, prod bundle byte-unchanged) + `toHaveCSS` retry + `emitAndAwait` for the emit-loss family; stress 0 flakes at repeat-each 8/10; **γ SUCCESS on `64669944`** | — |
| api-logs-redact | `/api/logs` (routes/logs.py) streams raw journalctl with ZERO redaction — secondary leak path, independent of SEC-mcp-clientid | ✔ | SEC | — | merge `2b5662d6`+`d1c78d7b` (lane `848fb212`, Phase-1 wave-1) | redactor extracted to shared `hal0.api._redact` (no mcp-SDK dep for a core route; `admin` imports it — one owner), `redact_log_line` wired into both `list_logs`+`journalctl_sse`, `_KEY=` pattern added; tests/api redact+logs green (236 combined w/ mcp) | ⚠ third leak `/api/slots/{name}/logs/stream` (`hal0.slots.logs`) still unredacted → follow-up row |
| cli-auth-streamtest | explicit auth-on smoke tier exercising every streaming verb against a keyed TestClient (§5.1 asked; CLI-auth leaned on transitive `_auth_headers` coverage) | ✔ | CLI | CLI-auth✔ | folded into `cli-verbs` merge (Phase-1 wave-1) — `tests/cli/test_cli_auth_streamtest.py`: real app, `require_auth=on`, real admin key, every streaming verb driven through it (401 no-key, pass keyed) | — |

#### Phase 1 — sync wiring — **COMPLETE** (11 Sonnet worktree lanes across 3 waves, Opus-reviewed, batched cherry-picks). Wave 1 CI-GREEN `43ea03d0`; Wave 2 `bc2954e4`..`0941df5f` (γ green, python pending); Wave 3 `7a81af9d`..`67d1ddea`. Consolidated gates green each wave (wave-1 mcp+redact+logs 236 / cli 601[1 rocminfo]; wave-2 675 targeted[1 dist-toggle allowlist artifact, see row]; wave-3 tsc+build + γ security/model-drawer 7/7 + lane e2e 10/10+42/42, title `v0.9.8`). DoD met: no shipped surface references a dead route/verb/tool; CLI reaches merged R3/R4 abilities; admin MCP exposes tier-a; VERS-flash + NAMES-stale closed; contract drift reconciled. Next: Phase 2 (structural + seamless drawers).
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| MCP-sync | §4.3 tier-a admin catalog buildout: 12 tools (slot by-name/by-id/resolved/state reads, system_info, memory_recall, slot_set_defaults PATCH, model default/duplicate, model_pull_delete, slot_rename gated, bench_queue_delete gated) + memory_search/list/recall reclassified read + `EXCLUDED_TOOLS` name→reason table + `_validate_catalog` collision guard | ✔ | API | Phase-0 MCP-patch✔ | merge `645aa490` (lane `31f4c9f3`) | Sonnet-built, Opus-reviewed exposure classification (reversible→autonomous, destructive/redirect→gated, reads→read); `brain.chat` `_ADMIN_TOOL_EXCLUDES` += memory_recall; tests/mcp 168 lane / 236 combined green; route-sync pin holds | — |
| CLI-auth+verbs | §5.2: `hal0 auth {status,rotate,require}` + `slot rename` + `model default[--clear]`/`update[--check]`/`pull --cancel` + `hal0 ports` + `hal0 board {list,show,add,move}` + `hal0 chat --brain`; `model import-backup` now chains `import_toml_to_sqlite` (was inert post-ML-1); killed 2 stale "daemon has no auth" claims (cli.mdx + agent_commands) | ✔ | CLI | Phase-0 CLI-auth✔ | merge `21153e43` (lane `3fa9f0e0`) | Sonnet-built, Opus-reviewed; every verb through Phase-0 `_shared.auth_client`/`api_stream` (no bespoke client); new auth/board/ports command modules + 6 test files; tests/cli 601 pass (1 pre-existing rocminfo-env fail, confirmed on clean stash) | — |
| UI-endpoints | SWEEP §2: consolidate `ui/src/api/endpoints.ts` — delete 13 zero-ref dead consts, wire 3 literal-shadowed consts (statsPower/comfyuiPreview/comfyNative{Queue,History}) to their consumers (kills the dup literal), KEEP-DOC agentApprovals*/doctor (future-reserved) | ✔ | UI | — | merge `024147fb` (lane `f765a05d`) | Sonnet-built, Opus-reviewed diff; tsc+eslint+build clean, e2e comfyui+services 16/16, vitest 3/3 | — |
| VERS-flash | §3: `ui/index.html` hardcoded `v0.5.0-alpha.1` flashed stale + repo version split (pyproject 0.9.8 vs ui). Single-source: package.json→0.9.8, Vite `versionHtmlPlugin` stamps `%APP_VERSION%` pre-paint, `document.title` live-synced from `/api/updates/state` post-mount (AboutPage pattern) | ✔ | UI | — | merge `dbf9a631` (lane `83bb5102`) | Sonnet-built, Opus-reviewed; built `dist/index.html` shows `v0.9.8`, no alpha, no leftover placeholder; 2 new e2e; other version literals confirmed FLM/demo fixtures (untouched) | real release number = Phase-5 decision |
| _(api-logs-redact, cli-auth-streamtest: see Phase-0 table — closed in wave-1)_ | | ✔ | | | | | |
| missing-routes | §1.2 three declared-but-missing GET routes: `/api/stats/requests` (dispatcher rollup via new `metrics_read.requests_rollup`, CLIENT via `/api/stats` prefix), `/api/doctor` (verdict feed; classifier logic hoisted `cli.doctor_verify`→pure `hal0.health_report` so api/ never imports cli/; ADMIN GET), `/api/auth/exposure` (serializes RULES+OPEN_ALLOWLIST live; matchers gained read-only `.pattern`/`.kind` to avoid path-string dup; ADMIN) | ✔ | API | MCP-sync✔ | merge `0941df5f` (lane `4a5dc6d5`, wave-2) | Sonnet-built, Opus-reviewed exposure (both new routes ADMIN, deny-by-default intact, no new OPEN); Pydantic response models; explicit ADMIN rules added; 675 targeted pass; NOT `/api/migrations/flag-report` (FLAGS-own turf) | — |
| NAMES-stale | §3: reported primary/legacy reload-flash — INVESTIGATED, flash already dead (legacy views render skeletons, prod DCEs the mock — verified in dist bundle). Real latent bug found+fixed instead: name-literal selectors checked `name==='primary'` before `isDefault`, and bare `.find(isDefault)` could grab the wrong group's default (every group's default carries isDefault). Now role/type-filtered first | ✔ | UI | — | merge `8a95b8c1` (lane `12cbe226`, wave-2) | Sonnet-built, Opus-reviewed; `useSlots.inferSlotShape` + agents-overview + dashboard-redesign selectors role-keyed; fixture strings kept (15+ specs depend, proven unreachable); new live-shaped e2e 2/2; tsc+build+83 specs green | — |
| slot-logs-redact | third raw-journal leak path (surfaced by wave-1 api-logs-redact): `/api/slots/{name}/logs/stream` streamed unredacted | ✔ | SEC | api-logs-redact✔ | merge `bc2954e4` (lane `ed7b9613`, wave-2) | reuses shared `hal0.api._redact.redact_log_line` (one owner) on every streamed line; regression test verified fails-pre-fix; stale logs.py docstring corrected; tests/api+slots 195 pass | — |
| open-allowlist-test-fragility | `test_open_allowlist_is_exact` toggles pass/fail on `ui/dist` presence (SPA static routes `/assets`/`/brand`/`/favicon.svg`/`/{full_path}` mount only with dist, aren't in OPEN_ALLOWLIST); CI python job runs no-dist→green, local-with-dist→red | ☐ | CI | — | surfaced by missing-routes gate | P4-tests: make the test deterministic (always-mount or always-skip static) — benign, no security impact (routes correctly OPEN, `Missing:[]`) |
| dup+mock-realign | §1.2/§2: DuplicateModelDialog wired to real `POST /api/models/{id}/duplicate` (new-id input, error/pending states) + mock realignment — `buildCapabilities` now matches real orchestrator envelope `{backends,catalogs,selections}` (stale shape was starving Voice/ImageGen catalogs in mock mode), dead auth rows removed, revocation fields added | ✔ | UI | UI-API-1✔ | merge `c156f81f` (lane `3811273f`, wave-3) | Sonnet-built, Opus-reviewed; tsc+build clean, vitest 3/3, playwright 42/42 | — |
| UI-API-2 | §1.2: `ExposureTable` wired live to `GET /api/auth/exposure` (new `useAuthExposure` hook → `ENDPOINTS.authExposure`); found+fixed a real bug (backend `auth_class` lowercase vs uppercase chip-lookup → always painted ADMIN); client-key-set + login-throttle stay honestly gated (verified backend routes genuinely absent) | ✔ | UI | missing-routes✔ | merge `55d1da47`+`67d1ddea` (lane `58d6091b`, wave-3) | Sonnet-built, Opus-reviewed; tsc+build clean, security e2e 10/10 (+7/7 combined) | — |
| CONTRACTS-sweep | §2: reconciled endpoints.ts/CONTRACTS.md self-contradictions (doctor/stats/rotation "missing" comments false post-waves) + added authExposure/statsRequests/doctor consts; killed WS param lie (`?token=&tenant=` frozen but real board WS reads `since`/`board` — board off Hermes proxy onto local SQLite); flagged 2 unused board routes as doc notes (not dead consts) | ✔ | UI/DOCS | — | merge `7a81af9d` (lane `6bae912b`, wave-3) | Sonnet-built, Opus-reviewed; tsc+eslint+build clean; facts hand-verified vs board.py/board_ws.py/doctor.py/auth.py | — |

#### Phase 2 — structural decomposition + seamless drawers (the release core) — IN PROGRESS. Wave 1 merged `a9b1ea92`+`0b2417e9`, Wave 2 merged `15b9d344` (all gate green). Serialized ahead: P3-routers inc3 remainder (typed-bodies — needs dashboard-key status-code audit — + MCP route-map autogen standalone), FLAGS-own + DRAWER-shape (centerpiece, own reviewed wave), importer-flips/sunset-stamps, ESM/CSS consolidation.
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| lifespan-split | §3/§11: phase-split `lifespan()` (api/__init__.py, 540 lines→13 named ordered phases, byte-for-byte behavior) + loose `app.state` soup→typed `BootState` (30 fields, mirrored onto app.state so every reader works) + additive `BootReport` (per-phase ok/timing on `app.state.boot_report`). `create_app()` deliberately untouched (centrality is fixture noise). Subtle boot-order guard preserved+documented: events published AFTER auto-resume-pulls (which reads `getattr(app.state,'events',None)` expecting None) | ✔ | API | — | merge `a9b1ea92` (lane `599b2939`, wave-1) | Opus-built, Opus-reviewed (app.state mirror + ordering guard verified in diff); boot smoke (30 fields + BootReport 13 phases + clean shutdown + /health 200); tests/api 1417 lane / 174 merge-subset pass; ruff/sunset green | — |
| P3-routers-inc3-pull | spec-p3-routers.final.md §5 step 9: extract pull/update-pull orchestration sequencing (inlined in 7 route handlers) from `routes/models.py` into `registry/pull_jobs` service fns (enqueue/enqueue_update/list_all/status/cancel/delete/build_stream_response); handlers now thin parse→call→render. models.py **1145→890** (−255; DoD ≤550 = further inc3 steps) | ✔ | API | lifespan-split✔ | merge `15b9d344` (lane `aa0d4fbd`, wave-2) | Opus-built, Opus-reviewed (all monkeypatch re-export seams preserved — `_run_pull_with_events`/`_schedule_pull_task`/`_resolve_pull_capability`/etc kept as module bindings, `run_wrapper=_run_pull_with_events` injected so patches land; `pull_jobs.get_curated`/`run_pull` still patch targets); paths/methods/status/payloads/exposure byte-frozen; ruff+sunset(200) green, create_app 342 routes, tests/api 1417 lane / 271 merge-subset pass | — |
| settings-seam | spec-settings.md: audited all 19 settings pages — ALL already on the typed seam (settingsClient/useSettingsForm or a dedicated typed hook), nothing left to migrate. Wired the 2 SWEEP-§5 placeholder pages FOR REAL (not demoted): HealthStatsPage → 5 live panels (health/hw/power/`/api/stats/requests`/services via existing fail-soft hooks); LibraryDownloadsPage → catalog count + reuse of models.jsx DownloadsPane (one-owner) + link-out. No "not yet wired — placeholder" string left in ui/src | ✔ | UI | — | merge `0b2417e9` (lane `f322e66f`, wave-1) | Sonnet-built, Opus-reviewed; tsc+eslint+build clean; settings γ 10/10 solo-worker; 2 new mount tests assert placeholder string dead | ⚠ 3 settings specs flake under parallel workers (incl. document.title version test) — confirmed pre-existing on clean base, CI-green; watch for recurrence |

#### Phase 3 — memory/Hermes finish + SWEEP (wave-1) — merged `dc0f5cf0`→`577ee462` (4 lanes, batched cherry-pick, one push). Consolidated gate green: ruff+format clean, sunset 200≤200 flat, create_app 342 routes, targeted pytest union **410 pass / 1 xfail** (pre-existing SECURITY item-4 terminal.backend deviation — recorded, not new). All 4 lanes commit direct onto `dc0f5cf0`, file-disjoint. Base-drift caught+corrected in-lane (3 worktrees spawned stale `f2d82d38`, reset to tip per gitops). Held for next wave: FLAGS-own centerpiece, typed-bodies (dashboard-key audit first), MCP-autogen. Decisions owed to user batched below.
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| MCP-mem-hindsight | rename plugin tools `hal0_memory_{search,recall,add}` → upstream `hindsight_{recall,retain,reflect}` (old names kept as dispatch aliases; search folded into recall) + implement `hindsight_reflect` (client-side synthesis via `_client.recall` types-hint, empty-on-fail) + move config to `~/.hermes/hindsight/config.json` `local_external` shape + fix false "only copy now" docstring in both copies + tests | ✔ | HERMES | — | merge `4c2e5f14` (lane `dbbfa930`) | Opus-built, Fable-reviewed (dispatch aliases + config back-compat + parity verified in diff); BOTH byte-parity copies edited identically (`diff -r` clean, seed-parity test green); provider-level `reflect()` left no-op per scope (plugin tool is standalone); plugin+client+parity 134 lane / merge-union green; plugin.yaml already 1.2.0 kind:exclusive | ⚠ old-path config `$HERMES_HOME/hal0-memory.config.json` no longer read on live boxes (non-crashing — env supplies agent_id/base) → migration note owed |
| brain-lane-relocate | move 5 `RELOCATE(brain-lane)` steps out of `install_hermes` `_INSTALL_STEPS` into the api lifespan: `persona_seed`+`brain_profile_mcp_wire` fold into `_boot_seeds` (#10, local FS, persona dedupe vs existing seed); `namespace_register`+`brain_profile_seed`+`self_report` → NEW terminal phase `_boot_brain_lane` (after #13); `_BRAIN_LANE_STEPS` frozenset + tripwire reworked | ✔ | HERMES | lifespan-split✔ | merge `577ee462` (lane `2600bba3`) | Sonnet-built, Fable-reviewed (boot-critical). **Real catch:** loopback HTTP to `/api/memory/*` is connection-refused during ANY boot phase (uvicorn binds socket AFTER lifespan.startup) → rewrote to in-process `memory_provider` dispatch bridged sync→async via `to_thread`+`run_coroutine_threadsafe` (non-deadlocking: phase body in threadpool frees the loop). All 3 terminal steps try/except warn-as-OK, never block boot. Live boot verified 14 phases all-ok, brain_lane last, degrades clean when memory down; tests/api 1417 pass, hermes_provision 175 pass | ⚠ behavior changes: `agent install hermes --repair` no longer force-resets personas to canonical (only relocated step had it); `self_report` now summarizes boot-phase failures (labeled substitution, no smoke-test analogue at boot) — both owed to user |
| drift-watch+hermes-bump | add drift-watch fixtures for the newly-consumed Hermes surface (`kanban_db.py` 7-table roster, kanban runs API, session-token injection seam) to `tracked_files` + contract fixtures; author `hermes-bump` runbook (bump procedure across all pin locations) | ✔ | HERMES/DOCS | — | merge `23c92424` (lane `2b6ee583`) | Sonnet-built, Fable-reviewed. **Real gap found:** `HermesBoardExecutor.dispatch/cancel` assume `POST /api/plugins/kanban/runs`(+cancel) routes that DON'T exist in the pinned Hermes (only GET/inspect/terminate/batch-dispatch) — froze what upstream ACTUALLY ships + a canary test that fails loudly if upstream adds them (matches Phase-6 "one-constant fix" anticipation), did NOT freeze hal0's unverified assumption. Surfaced a 4th pin location `VETTED_HERMES_REFS`. drift suite 29 pass/1 xfail, broader agents+board 820 pass; runbook `docs/rework/hermes-bump-runbook.md` (287 ln, 9-step procedure + rollback) | — |
| SWEEP-delete (wave-1) | delete 4 grep-confirmed-dead symbols: `_idle_monitor_loop` (slots/manager.py), `_all_known_drivers` (agents/manager.py, +orphaned Iterable import), `_parse_flm_version` (routes/updater.py, superseder kept) | ✔ | SWEEP | — | merge `7e062603` (lane `69e63171`) | Sonnet-built, Fable-reviewed (clean deletions, superseder retained). 4th candidate `dry_run` param SKIPPED — correct judgment: found live back-compat test `test_migrate_deprecated_dry_run_flag_is_still_a_noop_preview` pinning the documented noop contract. Each delete re-grepped 0-callers just before removal; import smoke + sunset(200) + ruff + 182 targeted pass | — |
#### Phase 3 — wave-2 (decision-driven, from wave-1 phase-boundary batch) — merged `8ffc990a`→`5bf5a743` (3 lanes). Consolidated gate green: ruff+format, sunset 200 flat (first real HAL0-SUNSET stamps, not overdue), create_app 342, targeted pytest union **206 pass / 7 skip** (pre-existing provider-default skips). All 3 file-disjoint. User decisions applied verbatim (see rows).
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| reconcile-wire | USER DECISION=wire (not delete): `reconcile_listeners` (ports/authority.py:375) wired into `GET /api/ports` (`routes/ports.py`) so the reconcile pass runs — orphan pool listeners now surface as `{port, owner_kind:listener}` entries appended to `authority_claims` | ✔ | API | — | merge `cdb9ef83` (lane `266b4dd2`) | Sonnet-built, Fable-reviewed: return-shape verified pass-through-safe (`port_report` sets `report["authority_claims"]=…` no per-entry validation; partial listener dict is the intended minimal orphan surface); `contextlib.suppress` failure guard (reconcile probe error degrades to unreconciled claims, never 500s); regression test proven to FAIL pre-fix (stash-check); 18 pass | ⚠ doubled psutil `net_connections` scan per `/api/ports` (admin low-freq read — fine; flag if pulled into a poll loop) |
| sunset-ratchet | USER DECISION=ratchet all→next release: first real `HAL0-SUNSET: v0.10.0` stamps (12 items/13 lines): `--backend` flag ×2, `SlotConfig.runtime`, `SlotConfig.workers`, `cognee` engine literal, 5+2 CLI aliases (probe/registry-import/register/assign/set-credentials/slot-add/remove), `bundle_chosen_marker` (picker deferred, marker stamped) | ✔ | SWEEP | — | merge `48a9b48a` (lane `6d020d8b`) | Sonnet-built, Fable-reviewed: stamps-only, zero behavior change; check_sunset GREEN both checks (version tuple (0,10,0)>(0,9,8) → not overdue; scar count held at 200/200 by wording reasons around the trigger regex, baseline untouched); 240 pass | v0.10.0 target derived from CHANGELOG semver policy (removals=breaking=minor bump) — **re-target if the release number lands elsewhere** (Phase-5 decision owed) |
| persona-flag | USER DECISION=keep --repair non-destructive, add separate overwrite flag: new `--reset-personas` on `hal0 agent install hermes` force-overwrites default personas to canonical (the behavior brain-lane relocation retired from `--repair`); reuses `_phase_persona_seed(repair=True)`, zero dup | ✔ | HERMES/CLI | brain-lane-relocate✔ | merge `5bf5a743` (lane `11f9941d`) | Sonnet-built, Fable-reviewed: flag absent → idempotent (unchanged); present → canonical overwrite even over a divergent persona file; targets the same HERMES_HOME the boot lifespan resolves (`paths.var_lib()`); 49 pass + 562 broader | — |
| SWEEP-verify | 2 forgotten wire-ups: `reconcile_listeners` → **wired** (reconcile-wire ✔ above) · `bundle_chosen_marker` → USER DECISION=leave marker + stamp deprecated (backend bundles module KEPT — installer/hardware use it; only the unbuilt first-run picker routes/UI deferred) → stamped in sunset-ratchet ✔ | ✔ | SWEEP | reconcile-wire✔, sunset-ratchet✔ | — | both dispositions landed this wave; bundles picker = deferred feature epic (backend catalog + tiers + eligibility already tested, only routes+UI+marker-drop unbuilt) | — |
| SWEEP-settings | dead-settings + CLI-alias sunset → USER DECISION=ratchet all to v0.10.0 → landed in sunset-ratchet ✔; `rope_freq_base` drawer exposure kept (INTENTIONAL, Issue #548) | ✔ | SWEEP | sunset-ratchet✔ | — | version blocker + filler already shipped (0.9.8 clean); rope-drawer NOT stripped per #548 | — |

#### Phase 3 — wave-3 (the centerpiece) — merged `b486d7e3`→`0a0bc6e8` (FLAGS-own + stamp re-target). Consolidated gate green: ruff+format, sunset 200 flat (all stamps now v1.0.0, not overdue), create_app 342, targeted union **245 pass / 7 skip**; lane's own run 1284 pass (3 pre-existing env fails confirmed on main). Next release pinned **v1.0.0** (user) — sunset stamps re-targeted v0.10.0→v1.0.0; pyproject/UI version bump deferred to the Phase-5 cut.
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| FLAGS-own (§2 flag-text fold) | flags own by MODELS: argv resolution chain (`providers/container.py`) strips `profile`/`slot_overrides`/`extra_args` segments; `resolve_profile_flags` NOT called at launch (golden #5 invariant); `model.defaults` carries materialized flag text; MTP bundle re-injected model-driven; profile.image + slot image-override deprecated (v1.0.0 shims) — images belong to runners | ✔ | MODEL/API | SLOT-B✔ (91b55ad0+quoting cd5e091b), ML-4/5✔, P3-ui-dataseam✔, UI-D1-D3✔ (`2cfec5e1`) | merge `d4253f8f` (lane `26e84f44`) | Opus-built, **Fable-reviewed (launch+security)**: SECURITY IMPROVED — `UNTRUSTED_SEGMENT_LABELS` now `{extra_args, model_extra_args}` (was just `extra_args`), closing the §21.7/UI-API-1 gap where a model's `defaults.extra_args` bypassed `MANAGED_ARGS_DENYLIST`; managed `-ngl`/`-c`→typed fields (hal0-rendered, trusted), all freeform screened at `resolve_argv`; quoting fix (cd5e091b) untouched. 2 latent bugs caught (MTP silently lost; profile `-ngl 999` overriding slot typed ngl). Schema: option-(b) deprecated shims + `extra="allow"` → stale live-TOML slot fields load w/o boot crash. 1284 pass | migrator run = deploy-window |
| FLAGS-migrator | `config/migrations/slot_flags_fold.py` (NEW) — one-shot fold of slot effective-tune into model text, provenance=source profile; managed-flag split (`-ngl`/`-c`→typed, never extra_args; rest→extra_args; `--parallel`/`--kv-unified` folded); **DIVERGENT-SHARE REFUSAL** (multi-slot→one-model divergent tunes → refuse+report, apply raises, no partial write); idempotent, dry-run default, `apply` needs `deploy_window=True` | ✔ (code) | MODEL | FLAGS-own✔ | (same commit `d4253f8f`) | pure planner + guarded applier; precedence bug caught (profile text vs slot typed); 190 migrator tests | **NOT RUN — rides P2-config/deploy window** (spec §5) |
| FLAGS-own §7 tail (reconciled → superseded) | prior §7 plan folded ALL of slot `device`/`chat_template`/`mtp` into the model. Reconciled by axis: `chat_template` fold SHIPPED; `device` axis was the open item | ⏸ superseded | MODEL | FLAGS-own✔ | spec §7 → **superseded by `spec-hw-slot-ownership.md`** | **`chat_template` → model SHIPPED** (PR #1325, slot→model, divergent-share refusal, `HAL0-SUNSET: v1.0.0`). **`slot.device` → model runner-axis removal CANCELLED** (was task #23): the hw-slot-ownership pivot (2026-07-19 user directive) REVERSES the axis — hardware/placement stays on the SLOT, the model becomes logical/device-agnostic. §7's ~10-file GPU-visibility rewire + its live-GPU validation gate evaporate. `mtp` stays a typed slot toggle. See the hw-slot-ownership row below. | — |
| hw-slot-ownership | **PIVOT — supersedes spec-flags-ownership §7 (reverses the axis).** Hardware/placement → **SLOT** (typed 4-field HW grid `device · NGL · THREADS · BINARY` + optional `image_pin`); logical, device-agnostic tune (freeform flags + typed caps) → **MODEL** (no image/runner/device/-ngl/--threads); profile = device-agnostic tune template only (`profile.image` deleted); image resolution = `RUNNER_IMAGES[slot.binary]`, `image_pin` overrides. One-owner rule: every physical fact has exactly one writer (the slot). | ▶ | MODEL/SLOT/API/UI | FLAGS-own✔ (§2 flag fold `d4253f8f`), §7 chat_template✔ (#1325) | spec `docs/rework/hal0-specs/spec-hw-slot-ownership.md` (ratified 2026-07-19) | **Shipped:** Lane 0+A schema (`SlotConfig` +`n_gpu_layers`/`threads`/`binary`/`image_pin`; `RUNNER_IMAGES` +`supported_backends`/`format_arch`); Lane E UI (slot HW grid + fit-check warn, card device/NGL + image_pin chips, model-drawer removes device/runner/image + client HW-flag reject, Profiles drop `image`, Runtimes panel reverse-indexes SLOTS via `binary`); Lane C.1 API (system-info `supported_backends`, slot serialize+accept of the 4 HW fields, server-side fit-check WARN). **Lanes B/C/D SHIPPED (verified on `main` 2026-07-26, row was stale):** Lane B = `SLOT_HARDWARE_FLAGS` frozenset + segment hard-reject at `resolve_argv` (`slots/argv.py:114,246`), image chain `slot.image_pin or RUNNER_IMAGES[slot.binary]` (`providers/container.py:230-305`); Lane C = model/profile hard-reject wired at `services/models_service.py:1183-1234`, `api/routes/profiles.py:129-167`, `profiles/__init__.py:328-344`; Lane D = `config/migrations/hw_slot_ownership.py` (all four folds, idempotent, deploy-window-gated) + `hal0 slot migrate-hw` CLI. **Lane F open:** unstamped compatibility sites remain — `db/migrations/001_registry.sql:18,33` (`preferred_runner`, `n_gpu_layers`), profile-image drop `config/loader.py:529-540`, profile rationale `config/schema.py:1083-1089`, slot legacy-image `config/schema.py:444-447`. | migrations ride the deploy window |
| sunset-retarget | wave-2 stamps v0.10.0→v1.0.0 (release pinned 1.0.0) | ✔ | SWEEP | sunset-ratchet✔ | merge `0a0bc6e8` | 8 files, stamp-version only; check_sunset green (v1.0.0>0.9.8 not overdue) | version-string bump deferred to Phase-5 cut |

#### Phase 3 — wave-4 (deploy fix-forward) — 3 lanes fixing defects from the 2026-07-19 live install-validation (`docs/rework/deploy-validation/`). Consolidated gate green: ruff+format, sunset flat, create_app 342, both installer scripts `bash -n` clean, 135 targeted pytest pass. cli-hermes built off stale `f2d82d38` but 3-way cherry-picked clean + verified coherent with persona-flag's agent_commands.py. User decision = fix all. Real-box re-check of the installer gates owed post-merge (150).
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| quadlet-restart (m2) | slot quadlet emitted `StartLimitIntervalSec`/`Burst` in `[Service]` → systemd "Unknown key", silently dropped → restart rate-limit never applied. Moved to `[Unit]` (providers/container.py) | ✔ | RUNNER | — | merge `205d5262` (lane `fa950ca1`) | Sonnet-built, Fable-reviewed: correct per systemd.unit(5) (StartLimit* is [Unit]-only); rendered-unit proof; test `test_startlimit_keys_land_in_unit_section_not_service`; 812 pass (4 pre-existing :z env fails) | validated live post-merge |
| installer-gates (M2+M3) | M2: runtime-preflight gate captured no podman output → always blamed nesting/keyctl; now greps smoke output for keyring/EDQUOT + emits accurate remedy. M3: GPU gate accepted renderD128 gid→*any* group; now requires group==`render` + hal0 membership (hard-stop in LXC on wrong group, warn if user not-yet-member since usermod runs post-preflight) | ✔ | INSTALL | — | merge `6d154bf1` (lane `683532b2`) | Sonnet-built, Fable-reviewed: bug-encoding test fixed + 6 new; 60 installer tests pass; **M3 will now correctly hard-stop 143 (gid993→clock, real GPU misconfig)**; shared gate = same fns for 150/143, 150-passing-shape verified. ⚠ real-box install.sh e2e re-check owed | 150 re-validate post-merge |
| cli-hermes (m4+m1) | m4: `hal0 agent status hermes --json` had NO --json option → UsageError to stderr, empty stdout; added it. m1: DIAGNOSED real install bug (not doctor false-positive) — gateway-install ran as root → stray root-owned `/root/.hermes`; dropped both call sites (install.sh:2188, agent_commands.py) to hal0 user | ✔ | CLI/HERMES/SEC | — | merge `0aa0ca04` (lane `633525ac`) | Sonnet-built, Fable-reviewed: m1 root-cause airtight (code's own hint text names the correct `sudo -u hal0` invocation; #843 root-clobber class); new test asserts gateway-install argv routes through `_run_as_hal0`; 210 pass | m1 doctor-perms live re-check owed on 150 |

#### Phase 4 — deploy hardening (code fix-forward from the live deploy window). Gate green: ruff+format-check, sunset flat, create_app 342, targeted pytest pass. File-disjoint, batched.
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| m3-renderer | `providers/_gpu.py` `resolve_gpu_group_ids()` now derives render/video GIDs from `os.stat(device node).st_gid` (fallback name→probe→constant) — fixes the name/owner-gid divergence the 143 live run exposed (renderD128 gid993=clock vs getgrnam render=991); non-root/rootless slots now get the correct group | ✔ | RUNNER | — | lane `9fa55044` | Sonnet-built, Fable-reviewed; new `_device_node_for_group()` helper; 143 mismatch repro'd→fixed; 9+210 tests pass; `ruff format --check` clean | validate live at next deploy |
| migrator-ctx | `config/migrations/slot_flags_fold.py` drops launch-shadowed `context_size` from `FoldedTune` fold + divergence key — ctx is slot-preserved at launch (`_resolve_context_size` returns slot override first), so refusing on ctx-divergence was spurious + clobbered defaults. Kills the benign 143 qtest/smoke refusal + the ctx-clobber-on-apply | ✔ | MODEL | — | lane `e99a0c2d` | Sonnet-built, Fable-reviewed; launch-shadow confirmed in code first; 14+663 tests pass incl. ctx-only-divergent-no-refuse; `ruff format --check` clean | — |

#### SLOT-B M5 id-flip — REHEARSED on 143, **BLOCKED on runtime-version sequencing** (2026-07-19). Offline flip executed cleanly (fresh backup → downtime → `migrate_slot_id_keying` exit 0, 11 slots name→id, layout verified, split-model refs preserved, idempotent). BUT on API restart the deployed **0.9.8 runtime is still NAME-keyed** → re-seeded name-keyed slots alongside the id-keyed layout = SPLIT-BRAIN (18 runners, 11 id-keyed ERROR + 7 re-seeded). RESTORED from backup → 143 back to clean 11/11 name-keyed healthy (hal0.db sha identical). Root cause = the id-keyed-runtime read-path (reads `<id>.toml`) is a LATER version not deployed — exactly the migrator docstring warning. **Correct order: deploy the id-keyed runtime FIRST, then flip.** Flip mechanics + recovery both proven. Held until the id-keyed runtime lands on the box; NOT attempted on 150 (same runtime). Record: `docs/rework/deploy-validation/2026-07-19-idflip-143-attempt1.log`. **Root-cause scoped (read-only investigation): the id-keyed on-disk read-path is GENUINELY UNBUILT** — increment-B id-keying is in-memory only (`manager.py` dict[int] + `_name_to_key`); the persist/read layer is 100% name-keyed (`loader.py:189/238` read `<name>.toml`; `manager.py:537/540/2652` `_config_file`/`_state_file`; `static_seeds.py:71` re-seeds `<name>.toml` = the split-brain trigger). The migrator (`migrate_id_keying.py`) is the ONLY writer of `<id>.toml` and its docstring names a future "P3-runtime-db lane / migration 006" as the consumer — which does not exist (DB migrations stop at `005`; no branch adds an id-keyed read). Completing SLOT-B id-flip = **building that P3-runtime-db lane** (switch loader/manager read+write+state+seed+reconcile from name→id on-disk key + identity plumbing + systemd `hal0-slot@<id>` unit rename). Substantial new increment, NOT small wiring. **Not a v1.0.0 blocker** (platform ships name-keyed; migrator is inert/unwired). Defer to a post-1.0 SLOT increment.

#### P3-runtime-db (SLOT-B finish) — Inc 0–3: bilingual name-or-id on-disk slot layer + seed split-brain fix. Design-first (architect plan) → Opus TDD build. Unblocks the id-flip that split-brained on 143 (the deployed runtime was name-keyed).
| id | lane | status | class | deps | commit / branch | verify | deploy_state |
|----|------|:--:|--|--|--|--|--|
| idkey-inc4 (seam+CLI) | naming-seam id-flip (`naming.py` `slot_instance_token`→id when cfg.id; 4 container.py consumers untouched) + `hal0 slot migrate-id-keying` CLI (pre-flight backup tar + active-units safety gate + `--dry-run`/`--yes`) + `list_slots` id-awareness (5 callers use cfg.name not stem) + operator runbook `docs/operate/slot-id-keying-migration.mdx` | ✔ | SLOT | idkey-core✔ | lane `f7e498ad` | Sonnet-built TDD, Fable-reviewed; 342 routes, 1972 broader pass (1 pre-existing env fail), sunset+ruff+format-check clean; caught+fixed a failing docs-parity test | **SLOT-B code COMPLETE** — last step: deploy bilingual runtime to 143 + re-flip = no split-brain |
| idkey-core (inc0-3) | `slots/layout.py` (new) primitives + **bilingual read** (`manager._iter_configured_slots` enumerator replaces the 3 glob-stem chokepoints; `_config_file_for`/`_state_file_for` id-or-name w/ half-migrated state fallback) + **id-keyed write** (`save_slot_config`→`<id>.toml` when cfg.id; rename in-place id branch; delete sweeps both) + **identity-keyed seed/reconcile/fold** (the split-brain fix — `seed_static_slots(existing_names=identity)`, fold no-ops on id-stems so no bogus digit rows) | ✔ | SLOT | plan (architect), design ratified (bilingual-read, operator-run, CLI=`hal0 slot migrate-id-keying`) | lane `3ce97a88` | Opus-built TDD, **Fable-reviewed** (safety cruxes: `is_id_stem`=isascii∧isdigit guards unicode-digit names; enumerator `assert not name.isdigit()` can never leak digit-as-name; seed identity-check). halo143 reboot INTEGRATION test (seed→fold→migrate→re-boot ⇒ no `<name>.toml` recreated, one identity row/slot, id-keyed iter, state hydrates from `<id>/state.json`). **1315 slots/config/install pass** (name-keyed byte-unchanged), 342 routes, ruff+format-check+sunset clean | inc4 (naming-seam `hal0-slot@<id>` + CLI + docs) then live 143 re-flip = no split-brain |

### Migration-window lanes (orchestrator-run live steps, NOT agents — plan-copy §migration)
| id | lane | status | class | deps | notes |
|----|------|:--:|--|--|--|
| P2-config | capabilities.toml → derived view; one apply engine | ☐ | MODEL | — | 3-release window + create-on-select |
| P2-memory | Honcho→Hindsight migrate per workspace, then ordered deletion | ☐ | HERMES | HP-memory | use existing `hal0 memory migrate --from honcho --to hindsight`; seed deterministic Honcho fixtures on fresh halo143 (or sanitized read-only LXC105 export); verify persisted `[honcho]` tolerance; never mutate LXC105 during rehearsal |
| P2-updater-b | one cosign+swap+rollback path | ☐ | INSTALL | — | Model B part 2 |
| P3-runtime-db | state.json/pull-jobs/events → SQLite (one table at a time) | ☐ | MODEL | SQLite | — |

### Code-debt lanes (not migration windows — no live-box step)
| id | lane | status | class | deps | notes |
|----|------|:--:|--|--|--|
| typed-bodies-rest | replace raw `request.json()` with typed pydantic bodies across the routers PR #1322 did not cover | ☐ | API | inc-3 ✔ | **Row opened 2026-07-26 — its ABSENCE was the defect.** The R5 summary called this "typed-bodies (audit banked)" and the Phase-4 handoff sized it at 24 sites / 12 files; the real count on `main` is **51 `request.json()` sites across 15 files** (`api/routes/`: models 9, memory 6, slots 6, memory_admin 5, updater 5, settings 4, installer 3, backends 2, services 2, proxmox 2, capabilities/profiles/stacks/config/dashboard_layout 1 each). Discipline from inc-2 applies: **byte-preserve status codes** (422 was kept over the spec's proposed 400 where they diverged) — a dashboard-key status-code audit comes first. Not a v1.0.0-alpha.3 item; deliberately not built in the hardening drive. |
| perms-table-converge | reconcile the OwnershipStore rows with what the code actually creates | ✔ | INSTALL | — | **Opened and CLOSED 2026-07-26.** A fresh install reported **5** `doctor perms` drift rows; it now reports **0** (verified on a clean Ubuntu 24.04 container, `hal0 doctor perms` → zero DRIFT). One root cause throughout: creators ran under the ambient umask, and directories under the setgid state root also inherited the setgid bit, so `doctor perms --fix` reconciled once and the next create drifted straight back. Fixes: STATE.md mode preserved across the rendered-context atomic write (`24c6c91a`); `secrets/`+`secrets/agents/` table pinned 0755→**0700** — `--fix` had been *widening* the token-EnvironmentFile dirs on every install (`5708aed0`); advisory `.lock` files created 0664 so both sides of the root/hal0 seam can take the flock — the halo150 `POST /api/slots 500` class (`4fef8a3f`); `HERMES_HOME` chmod 0700, it holds `hindsight/config.json` (tenant API key) and was world-traversable at 2755 (`4fef8a3f`); chat-template store seeded 2775 at the creator that actually runs on install (`f90d525f`). |
| slot-warming-never-ready | a second vulkan slot's llama-server never binds its port — container "healthy", slot stuck `warming` | ☐ | SLOT/RUNNER | — | **Opened 2026-07-26, found by the γ gate on CT163 (24.04, GPU passthrough OK).** The `agent` slot loads fine and serves real inference on :8081. A SECOND vulkan slot created by the gate reaches `warming` and stays there: `podman ps` reports the container **healthy** and up 9+ minutes, but nothing listens on the slot port — `curl :8084/health` → `000` while the working slot's `:8081/health` → `200`. hal0 is therefore CORRECT to hold it at `warming`; the runner never came up. **Not admission contention** — unloading `agent` did not release it. Two things to chase: (a) why llama-server fails to bind on a second slot, and (b) why the container healthcheck passes while the server is not serving, which is what makes this look like a hal0 state-machine bug when it is a runner problem. Blocks the vulkan/rocm γ rows even with the harness fixed. |
| release-gate-ready-poll | the tier-γ harness records a row FAILED before the slot finishes loading | ✔ | DEPLOY | `388253bd`, `db084175` | **FIXED 2026-07-26.** `wait_slot_ready()` polls `GET /api/slots/<name>` until ready/idle, bails on error, bounded by `HAL0_TEST_READY_TIMEOUT` (default 420s); the load call's exit status is ignored. Verified against a live box — it correctly waits through the intermediate `warming` state. Original finding: With the argv bugs fixed (`388253bd`), `slot create` succeeds and **both the vulkan and rocm test slots reach `ready` with a real model** — but the harness has already written the row as `fail`. `hal0 slot load` returns/times out (the CLI's own HTTP read timeout) before ready, and the harness treats that return as the verdict instead of polling `/api/slots/<name>` for `ready`. Observed: rows failed at ~24s, slots `ready` seconds later. Fix: poll for `ready` with a bounded timeout (first load also pays a multi-GB toolbox image pull — the `agent` slot took ~4 min cold, seconds warm). Until then the gate cannot pass even on a correctly seeded box. |
| release-gate-needs-seeded-box | the tier-γ gate cannot run on a fresh container — document the prerequisite (or teach the harness to skip unseeded rows) | ☐ | DEPLOY | — | **Opened 2026-07-26.** `scripts/release-check.sh --dry-run` fails on gate 5 until `tests/release-gate-report.json` exists, and `make release-test` against a freshly-installed box returns **7/7 rows FAILED** — vulkan/rocm (no model pulled), flm (no XDNA driver), moonshine/kokoro (toolbox images never pulled → 404), openwebui (`/v1/models` empty), updater (rollback 400, correct on a box that never updated). Preflight passes, so the harness works; it simply requires a **seeded** box. The gate logic is sound (validates schema, 24h freshness, future-skew, row-vs-summary coherence, ≥1 pass, `fail == 0`) — the problem is that this prerequisite is written down nowhere, so "run release-check before tagging" reads as achievable on any test box and is not. Either document the seeded-box requirement in the release runbook, or give the harness a `--skip-unseeded` mode that marks those rows `deferred` rather than `fail`. |
| mcp-dep-floor | decide whether `fastapi`/`starlette` need an upper bound | ☐ | API | F0 fix `66a78cb7` | `pyproject.toml` pins `fastapi>=0.115` unbounded; a fresh install resolved 0.140.0 / starlette 1.3.1, whose route-table layout silently unmounted the whole MCP admin surface. The walker is now version-agnostic (`admin._iter_live_routes`), so this is no longer urgent — but nothing pins the assumption, and the failure mode was a swallowed warning. Consider a regression test asserting the built route map is non-empty at boot, and/or a tested upper bound. |

---

## Next checkpoint base
R2.1 is accepted on `main` and `rework/descar` at `6aa565b8`. New R3/R4 lanes branch from this exact
base in isolated worktrees. The Hermes suite proceeds with Stage 1 (`HP-core` and `HP-role-api`), while
`HP-executor` remains gated on KB-4/5/6. No lane may recreate divergence by building on a pre-R2.1 base.

## Open review-driven adds (not yet lanes anywhere)
- **metrics-db split** — measure write-lock latency under concurrent pull+registry+metrics; split
  `hal0-metrics.db` if nontrivial (plan-copy L479 already sanctions).
- **golden-path harness earlier** — pull §21.11 ahead of remaining structural waves (review #7);
  live installer already found 2 bugs (NFS chmod, self-port) the capped suite missed.
  **Accepted (Fable plan review 2026-07-18): land the automatable subset (#9 rename, #10 delete,
  #14 api-restart, #15 no-Hermes) as CI-runnable integration tests BEFORE SLOT increment B merges;**
  halo143-only paths become a scripted runbook.

## halo150 deploy-validation results (2026-07-18 — full report: `halo150-r3-deploy-issues.md`)
- **Runbook Phases 0–5 COMPLETE.** Greens: fresh R3 git install, auth, doctor (post-O2 CLI-auth
  fix), slot CRUD, quadlet generation via 4.x compat, GPU inference, teardown, M5 rehearsal
  (id-keying + idempotence on copies), rename semantics (offline-gated, id/port stable), no-think
  via the supported field+normalize path, system-info real GPU (Strix Halo 116GB).
- **Fixed forward from box findings (all on descar):** O1 slot-list degradation `c1ea4519` ·
  O2 CLI auth `c1ea4519` · O3 shim readiness `fc4f6d8d` · O6 lock-file perms rows + O7 legacy
  static-unit cleanup `78c6dd1c` · O8 quadlet 4.x compat `5adf6e0f` · **O9 SECURITY: bundle leaked
  HAL0_ADMIN/CLIENT_KEY (bare `_KEY` suffix missing from _SENSITIVE_RE) `64c956df`**.
- **Open (low): O10** — deprecated `[server].extra_args` UX trap: bare double-quoted JSON is eaten
  by (correct) shlex semantics; current code renders quoted input correctly (repro-verified).
  Candidate: store-time validation warning for JSON-looking tokens.
- **Deploy rows:** podman-5 template refresh (R5); hermes-provision lane gains: AppArmor
  containers.conf preflight (unconfined LXC), stale agent drop-in cleanup (241/CONFIGURATION_
  DIRECTORY class), `bootstrap --repair` correctness (O3 ownership drift).
- **halo143 second-box findings (2026-07-18 late):** O1 degrade fix + doctor-auth proven in
  anger on a second substrate; probe-cache poisoning fixed `86589fd1`; root-owned rootless-podman
  HOME dirs fixed via perms rows `9e07c0d3`; **version gate proven bidirectionally** (150 =
  compat branch, 143 = native branch). OPEN: (a) **rootful/rootless image-store split** —
  system-info probes hal0's rootless store while slots pull rootful → `installable` lie; probe
  the store slots use (via seam) — needs a row/lane; (b) **native AutoRemove on unprivileged
  podman-5-in-LXC suspected in slot start failure + netns teardown race** — under live diagnosis;
  likely resolution: stop emitting AutoRemove entirely. **STANDING POLICY: deploy-affecting
  lanes validate on BOTH 150 (4.9.3/privileged) and 143 (5.7/unprivileged).**

## Fable plan-review adds (2026-07-18 — accepted "ok"; fold into waves)
- **Migration-number allocation (protocol add):** numbers assigned at DISPATCH, on this board.
  Allocated: **005 = KB-4/5/6 board · 006 = P3-runtime-db slot-state · 007 = metrics split (if
  triggered)**. Two files at one version = broken migrate.
- **state.json double-touch:** increment B's M5 renames `state.json` that P3-runtime-db later moves
  to SQLite — either fold the slot-state table into the SLOT wave or scope M5 so runtime-db never
  re-migrates. Decide at SLOT-B dispatch.
- **KB-2/3 needs a spec before HP-executor/HP-automation:** tool-tier classification (read/mutating/
  destructive), approval-gate, injection-resistance tests for tool-output→model→destructive-tool
  chains (same bar as HP-memory row).
- **§I hermes-provision convergent rewrite = explicit lane** (spec `spec-hermes-provision.final.md`;
  file still 5,368 lines) — not implied under "slim installer"; give it a row at R4 dispatch.
- **Core-without-Hermes proof goes continuous:** CI job/marker tier running core suite with Hermes
  extras absent (golden path #15 as a gate, not a one-off).
- **P4-tests markers pulled forward:** tag podman/systemd-dependent tests now → local capped verify
  can run `tests/api -m "not podman"`; natural home of the C7d flake fix.
- **Docs-reference ratchet:** CI grep resolving every doc/spec path referenced in tracked markdown
  (mirrors scar ratchet; keeps P4-docs honest).
- **`scripts/lane_verify.sh`:** encapsulate the capped gate (ruff check + format --check, import
  smoke, sunset, named pytest targets) — shrinks dispatch prompts, kills the forgot-format class.
- **CI env ≠ uv.lock (bit twice, 2026-07-18):** CI pip-installs a floating FastAPI (lazy
  `_IncludedRouter` app.routes) while local venvs sync `uv.lock` (0.136.x, eager routes) — caused
  the duplicate route-collision failure AND the gp15 route-set failure, both CI-only. Fold into
  P4-tests: install CI from the lockfile (or add a lock-matrix job); until then, never assert on
  `app.routes` shape — probe routes behaviorally.
- **Proposed, needs user decision at R4 planning:** demote HP-voice + HP-automation below the R4
  exit bar (finish line = "small, optional Hermes integration"); god-module LOC burn-down tracked
  per checkpoint (today 18,836 across the seven diagnosed modules).

## Folded from lxc105 live session (`/home/mint/hal0-105-changes-summary.md` — reference, NOT deploy state)
lxc105 (10.0.1.142) is the untouched live reference — never deploy there. These are the durable
code/requirement findings from that session; the live model-swaps/host fixes are context only.
- **container.py arg-quoting bug** — now a SLOT lane row in R3 (space-less JSON token loses quotes →
  slot crash). Real correctness bug; fix during increment B / P3-quadlet (both own container.py).
- **Reasoning-channel / no-think default** (SLOT + templates) — MiniCPM5/saber are reasoning models;
  under `--jinja` + default reasoning-format the answer lands in `reasoning_content` and `content` is
  empty unless `chat_template_kwargs:{enable_thinking:false}` is sent (currently only via the broken
  flag). Fix = the container.py quoting bug OR ship a `*-nothink.jinja` that defaults it internally
  (froggeric Qwen template already does this). Also consider `--reasoning-format` to strip the cosmetic
  empty `<think></think>` prefix. Fold into P3-brain + any chat-template lane.
- **Hermes API-server hardening** (HERMES/SEC) — the live Hermes API server was enabled on lxc105 with
  `API_SERVER_HOST=0.0.0.0`, a placeholder `API_SERVER_KEY=change-me-local-dev`, and terminal backend
  `local` (unsandboxed). The Hermes integration lanes (HP-*, §7.4 slimming) and the installer MUST:
  bind narrow / gate by real key (no placeholder), and never expose an unsandboxed local executor by
  default. Add to the Hermes suite security checklist + `security/exposure.py`/installer review.
- **Brain model assets** — trained `hal0-brain-sft` (f16) + `-fpx4`/`-fpx8` ROCmFP4 quants + profile
  `brain-agent-fpx` exist (registered on lxc105); noted on the P3-brain row. Serving image for ROCmFP4
  STRIX quants: `hal0-rocmfpx:c077206`. Deploy-to-halo143 decision is a live/migration step, not a
  code lane.
